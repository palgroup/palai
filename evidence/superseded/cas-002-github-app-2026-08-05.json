[
  {"release":"code-and-ship-0.1.0",
   "case":"CAS-002",
   "clause":"compose now passes the App through, `palai up` warns when a bound repository has no App and when one is HALF configured, the control-plane says so at boot, and the PRIVATE KEY IS A HANDLE: its bytes go to a 0600 file secret and what the environment carries is a container PATH",
   "now_false":"THE SUBJECT OF ALL FIVE HALVES IS GONE: `9ad0665d refactor(publish)!: delete the deployment-global GitHub App` removed the App itself, so there is no App for compose to pass through, none for a deployment to lack or half-configure, and no private key to hold as a handle. It was deleted as the WRONG SHAPE rather than as dead weight — one App could mint only for the repositories one installation covered and took its owner/repo from an environment variable, so a deployment serving several bindings opened every pull request against whichever repository that variable named. Half by half: `compose now passes the App through` — the variables are out of deploy/compose/compose.yaml's environment; `and when one is HALF configured` — applyGitHubAppEnv and gitHubAppPartiallyConfigured are deleted, so there is no half-configuration to refuse; `the control-plane says so at boot` — the two boot warning codes went with them; `the PRIVATE KEY IS A HANDLE` — the App key file-secret slot is deleted, and the property that sentence was really about (a credential's bytes never riding the process environment) is NOT abandoned but is now a different mechanism, a binding's connection_ref resolved through the encrypted store, and is deliberately not substituted in here. ONE HALF SURVIVES AND IS RE-POINTED RATHER THAN WITHDRAWN: ``palai up` warns when a bound repository' cannot publish' is true and is now proven where the answer lives — missingPublisherNotice reads the actual binding rows after the stack is up and names the ones carrying no connection_ref, and CAS-002 lists cmd/cli/internal/stack/up_repository_test.go:TestABindingWithNoWayToPublishIsNamed for it. What is false there is only the App half of the sentence: the question is no longer 'has this deployment got an App' but 'has this binding got a credential'.",
   "rested_on":"gitHubAppPartiallyConfigured",
   "rested_on_gone":true,
   "evidence":"`grep -rl 'gitHubAppPartiallyConfigured' --include='*.go' --include='*.yaml' --include='*.yml' --include='*.sql' --include='*.sh' --include='*.ts' --include='*.tsx' . | grep -v ./evidence/` -> 0 files (2026-08-05). All four proofs CAS-002 listed for this clause were deleted with the file that held them: `git show HEAD:cmd/cli/internal/stack/up_publisher_test.go` -> does not exist. The removal was measured before it was made rather than assumed safe — `9ad0665d`'s own message records the live stack carrying ZERO App variables while 20 bindings existed and 6 carried a connection_ref, so the App was not the thing publishing; it was the thing 14 bindings were waiting on and would have waited on forever. NOT EVERYTHING THE COMMIT CLAIMS TO HAVE REMOVED IS REMOVED, and it is recorded here so the next reader measures rather than trusts: the message says the variables are gone 'from the code, the catalogue, compose and .env.example', and `grep -rn 'PALAI_GITHUB_APP' --include='*.yaml' --include='*.go' . | grep -v ./evidence/` still finds prose in deploy/compose/compose.yaml:283 and cmd/cli/internal/stack/lifecycle.go:102, plus deploy/compose/compose.env.example:73 and two docs/operations pages. Those are comments describing a mechanism that no longer exists, not a surviving code path."},

  {"release":"code-and-ship-0.1.0",
   "case":"CAS-002",
   "clause":"the GitHub App is PER DEPLOYMENT (GAP-3)",
   "now_false":"THE CEILING WAS ACCURATE AND IS SUPERSEDED IN THE DIRECTION THAT IS EASIEST TO MISREAD: the limit was not widened, the thing carrying it was DELETED. There is no GitHub App, so 'per deployment' has no subject. This matters more than a lapsed sentence usually does, because a ceiling is what a later plan budgets against: a reader treating this one as live plans a deployment-wide credential and a per-deployment installation that cannot be configured, and would then discover that the real constraint today is the opposite shape — credentials are PER BINDING through connection_ref, and a binding without one cannot publish at all, which is a narrower and more local failure than the ceiling described. The honest replacement is not 'GAP-3 is closed': one App per deployment was a ceiling on WHICH repositories a deployment could publish to, and that ceiling is genuinely gone, while nothing here claims the per-binding path has been exercised against a real remote — that is the live leg in the row below, and it is still open.",
   "rested_on":"GitHubAppConfigured",
   "rested_on_gone":true,
   "evidence":"`grep -rl 'GitHubAppConfigured' --include='*.go' --include='*.yaml' --include='*.yml' --include='*.sql' --include='*.sh' --include='*.ts' --include='*.tsx' . | grep -v ./evidence/` -> 0 files (2026-08-05). That predicate is named rather than the App type because it IS the deployment-wide question this ceiling asserted — whether THE deployment had an App — and no code asks it any more. What replaced it asks about a row: missingPublisherNotice iterates the binding rows and tests each one's connection_ref, which is the per-binding shape. `9ad0665d` also deleted adapters/repositories/broker_github.go (11 symbols, all App) while repositories.RepositoryPublisher survived unchanged, because NewTokenBroker already implemented the interface and `p.Broker == nil` was already a handled state — the per-binding half was shipped and proven before the deployment-wide half was removed."},

  {"release":"code-and-ship-0.1.0",
   "case":"CAS-002",
   "clause":"§6 leg 5 — a REAL GitHub App (`PALAI_GITHUB_APP_*`) pushing to a REAL remote and opening a pull request GitHub itself reports as a draft — is opened by E22 and stays OPEN",
   "now_false":"THE LEG IS STILL OPEN AND THIS RECORD DOES NOT CLOSE IT — no captured receipt from a real remote exists, and the claim that only a real provider can answer 'GitHub itself reports it as a draft' is untouched. What is false is the MECHANISM the leg names, and that was not cosmetic: the leg was written as an APP leg, and tests/live/repository/live_test.go's two gates demanded PALAI_GITHUB_APP_ID, PALAI_GITHUB_APP_INSTALLATION_ID and PALAI_GITHUB_APP_PRIVATE_KEY_FILE. When `9ad0665d` removed every reader of those three variables it left the gates standing, so from that commit until this record the leg was UNCLOSABLE: an operator holding the only credential that now exists could not run it, the test skipped on variables nothing sets, nothing reads, and which its own body had stopped using — liveBroker goes through PALAI_LIVE_REPO_TOKEN, a variable introduced by that same commit. An open leg and an unreachable one read identically from the outside, which is the whole reason this is written down: the same commit correctly moved applyGitHubAppEnv's unsatisfiable warning to where its answer lives, and created this unsatisfiable gate two files away. The gates are corrected in the change that carries this record; the leg is now REACHABLE and still OPEN, and it will be closed by a token-authenticated push, never by an App.",
   "rested_on":"gitHubAppSettings",
   "rested_on_gone":true,
   "evidence":"`grep -rl 'gitHubAppSettings' --include='*.go' --include='*.yaml' --include='*.yml' --include='*.sql' --include='*.sh' --include='*.ts' --include='*.tsx' . | grep -v ./evidence/` -> 0 files (2026-08-05); the console surface that let an operator supply the App is gone with it, so there is no path by which the leg's own variables could be set meaningfully. The unreachability was MEASURED, not inferred: at HEAD 9ad0665d, tests/live/repository/live_test.go:297 (TestLiveApprovedPushAndDraftPullRequest, the proof CAS-002 lists for this leg) and :433 (TestLiveApprovedMergeRefusesAMovedHead) both opened with a loop skipping unless all three App variables were non-empty, while `git show 9ad0665d -- tests/live/repository/live_test.go | grep -c '^-.*PALAI_GITHUB_APP'` -> 10 deleted USES inside the same file. Both gates now demand PALAI_LIVE_REPO_TOKEN with the three PALAI_GIT_* values; the token is REQUIRED rather than optional because liveBroker falls back to an anonymous broker without it and an anonymous broker cannot push to a real remote, which would turn the one claim no fake can answer into a failure about credentials."}
]
