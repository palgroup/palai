# Palai Workspace, Repository ve Coding Journey Implementation Plan (E09)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Library-idiom-heavy adımlar (aws-sdk-go-v2 S3, go-git/git plumbing, OCI mount, cgroup limitleri) brief'lerinde Context7 grounding alır (repo politikası, ledger 2026-07-17).

**Goal:** E08'in durable interactive Session'ını `MASTER-SPEC.md` §29/§30 workspace + repository + coding journey'sine büyütmek: logical Workspace/Binding/Allocation + single-writer lease, exact-commit deterministic clone + untrusted-repo policy + scoped GitHub App credential broker, sandbox-backed file/shell tool yüzeyi, changeset + patch/test artifacts + secret scan, child worktree + explicit conflict-aware merge, model-facing tool yüzeyi (durable `task`/`agent`/`todo` registry + model-driven delegation), push branch + draft PR (exact approvals/idempotency/reconciliation) ve unsafe local bind — hepsi gerçek provider'ın gerçek **tool-use round-trip**'iyle canlı kanıtlanır (journey 63.2 kill'SİZ yarısı; kill+recovery E10).

**Architecture:** Yeni çekirdek icat edilmez. E08'in hazır bıraktığı üç şey tüketilir:
1. **Tam tool round-trip** — engine loop'u zaten `AWAITING_TOOLS` state'iyle model→tool→model döngüsü kurar (`engines/reference/src/palai_engine/loop.py`), controller `orchestrator.go:263` `tool.request`'i `dispatchTool` (`tool_dispatch.go`) ile yürütür, provider-one adapter'ı `tools` array + `ForceToolCall` destekler (`adapters/models/provider_one/adapter.go:249-266`). Production bugün TEK tool bağlar: in-process `ConformanceMathAdd` (`main.go:93`). E09 gerçek `Tool` değerleri KAYDEDER; tek mimari ek, tool-broker'ın in-process (`packages/tool-broker/broker.go:1,83`) olmasının kapattığı boşluk: **sandbox-backed execution seam**.
2. **ChildRun infra** — `child_dispatch.go` depth/fan-out/budget/capability admission'ı tam; `workspace_mode` alanı frame'de taşınıyor ama **kimse tarafından tüketilmiyor** (default `"none"`, enforce eden yok, schema'da bile yok — Go/Py-only). E09 onu model-facing yapar ve enforce eder.
3. **Command spine** — `approve`/`deny` kind'ları enumerate edilmiş ve API'de kabul ediliyor ama store'da `no_pending_approval` ile reddediliyor (`packages/coordinator/commands.go:194`). E09 ilk approval-ÜRETEN tool yüzeyini (push/PR) ekleyip bu switch'i pending-approval kaynağına bağlar.

LP-0'ın compose'da sağlıklı çalışan ama tüketilmeyen SeaweedFS'i (§7.2 carve-out) ilk kez yazılır; `oci.ContainerSpec`'e volume alanı eklenir (bugün workspace mount İFADE EDİLEMİYOR, `driver.go:36-43`). Yeni durable primitive'ler (workspace, changeset, `task`/`todo`, approval, publication) session/run/command kalıbında eklenir. Dependency direction korunur (master plan §5.1): engine DB/S3/credential GÖRMEZ; artifact store + credential broker yalnız control-plane'de.

**Master plan bağı:** §7 M5 Interactive Coding; §8 E09; §63.2 zorunlu coding journey (kill'siz yarısı bu plan, kill+recovery E10). LP planı §7.2 object-store carve-out'u ZORUNLU task olarak taşınır (T2). UAT sahipliği: REP-001..012; SAN-001..006 (SAN-005/006 E10 ile paylaşımlı — snapshot RESTORE ve host-kill yarısı E10, tıpkı E08'in SES-009/010 checkpoint yarısı gibi); SAN-010; SUB-006. Durable tool registry + model-driven delegation için formal task tasarımı + yeni UAT case'leri (REG/DEL/APV) master plan madde 410-411 mandate'iyle bu child planda authored (spec §64 kataloğuna reconciliation §7'de kayıtlı devir).

---

## 1. Execution gate

- [ ] `main` >= `300b47a` (E08 merged: durable sessions, commands/config-revisions, subagents/ChildRun, çok-client attach). E09 bunların hepsini genişletir; foundation olmadan başlanmaz.
- [ ] E08 evidence intact: `make evidence-verify RELEASE=interactive-0.1.0` PASS ve `make verify` yeşil bir baseline'dan başlanır. LP-0 evidence de intact (`make evidence-verify RELEASE=local-live-0.1.0` PASS).
- [ ] Her task RED-first TDD (master plan §14). Proof sınıfları master plan §10.2'ye göre atanır; **daha düşük sınıfla pass edilemez** (line 641): `live-provider` fake ile, `external-receipt` sahte remote ile, `fault-live` simüle-string ile GEÇMEZ.
- [ ] Her task gerçek-provider **LIVE smoke** ile biter (kullanıcı politikası; E08'de 5 gerçek-provider bulgusu yakaladı). E09'un LIVE smoke'ları gerçek providerın ilk **gerçek tool-use round-trip**'leridir (E08 memory: real runs single-step, tool yüzeyi real provider'a açık değildi — bu ceiling E09'da düşer, aşağıda T4 pivotu).
- [ ] Credential hygiene E08'den genişletilir: `.env.local`, `set -a` (asla `set -x`), hygiene grep artık `sk-` YANINDA Git PAT + GitHub App private key + installation token literallerini de tarar (T3/T8/T9); evidence/log/argv/snapshot'ta 0 credential.
- [ ] Branch: `feature/phase-09-repository-coding` (base: bu planın commit'i).

## 2. Bu dilimin sınırı

### Dahil

- **Logical Workspace/Binding/Allocation** (§29.7) + single-writer lease + fencing token (§29.8): `requested→provisioning→preparing→ready→leased` state machine; logical workspace id host-hareketinde sabit, her fiziksel allocation ayrı id + fencing token. Sandbox'a **workspace volume mount** (`oci.ContainerSpec`'e volume alanı — bugün ifade edilemiyor).
- **Deterministic clone at exact commit** (§30.3 11-adım preparation) + untrusted-repo defenses (§30.4: hooks disabled, submodule/LFS policy, symlink/case-collision, .git config override reddi) + **scoped GitHub App credential broker** (§30.2/§28.11: short-lived repository-scoped installation token, argv/env/URL/log/snapshot/model-context'te YOK, operasyon sonrası revoke; model asla raw Git credential görmez).
- **Sandbox-backed file/shell tools** (§28.7/§28.8): path traversal/symlink/device/socket reddi, before/after hash + changeset event; shell argv-form + sandbox user + timeout/CPU/mem/disk/process limitleri + process-group kill + secret redaction; container runtime/host socket erişimi yok.
- **Changeset** (§30.6) first-class immutable summary + patch/test artifacts (LP §7.2 object-store'un İLK gerçek consumer'ı — S3 write-path + Task 11d purge'ünün gerçek byte silmesi) + secret/license findings; model prose'u changeset'in yerine GEÇMEZ.
- **Child branch/worktree + explicit conflict-aware merge** (§30.5) + `workspace_mode` enforcement (child default read-only snapshot; mutable iş için isolated branch/worktree, parent workspace concurrent mutate edilmez).
- **Model-facing tool yüzeyi:** durable `task`/`agent`/`todo` registry (DB-backed, session-scoped, durable, multi-client görünür, context-recovery — AI "ne bitti/ne bitmedi"yi DB'den okur ve context dolsa/reset olsa bile kaldığı yerden devam eder) + model-driven delegation (`agent`/`spawn` tool_call → `child.request`; E08 T5 config-driven delegation MODEL-driven olur) + parent↔child model-facing mesajlaşma (`send_to_agent`/`agent_message` tool + child-idle event → parent model).
- **Publication:** push branch + draft PR ayrı capability/approval/credential/idempotency/audit ile (§30.8-30.12); `approve`/`deny` command'larının UYGULANMASI (E08 devri — ilk pending-approval kaynağı bu push/PR tool'larıdır); force-push/merge/protected-branch default DENY; lost-ack reconciliation + duplicate-callback tek PR.
- **Unsafe local bind** (§30.13): default snapshot/copy; direct mutable bind mount yalnız explicit unsafe-development flag'iyle + prominent warning + "sandbox isolation iddia edemez" + publication disabled.
- **Preview/terminal authorization** (§29.16): expired/wrong-tenant route reddi, doğrudan sandbox adresi expose edilmez (SAN-010).

### Dahil değil

- **journey 63.2'nin kill+recovery yarısı** (adım 8-9), workspace snapshot **RESTORE**, checkpoint/replay ladder, SAN-005 restore-half + SAN-006 host-kill-half, SAN-007/008 → E10 (`phase-10-recovery-replay.md`). E09 fencing/lease + snapshot CREATE + create-side checksum/exclusion'ı kanıtlar; host-loss ve restore E10'dur (E08'in SES-009/010 checkpoint yarısını E10'a bırakması ile aynı disiplin).
- **Parent-detached durable child + parent-child agent conversation'ın DURABLE yarısı** (child `waiting` idle, `run.restore` ile parent-detached long-lived) → E10 (master plan line 431). E09 yalnız model-facing yarısını (agent tool → child.request, send_to_agent, child-idle event) lands; detach + durable-conversation birleşimi E10.
- **DB-backed model routing + LiteLLM model adapter** → E06 (`phase-06-reference-execution.md`, LP §7.3). Journey adım 7'nin "ucuz route"'u E08'in `ModelRoute.Model` per-step efektif model-id'siyle (AYNI provider içinde) kanıtlanır; gerçek route/connection seçimi ve LiteLLM'in controller-side model ADAPTER entegrasyonu E06'nın işidir — **fork, bu plana katlanmaz**.
- **AgentProfile/AgentRevision, triggers, schedules** → E11. Durable `task`/`todo` registry AgentProfile DEĞİLDİR — session/run-scoped runtime primitive'lerdir; kalıcı publish-edilen AgentProfile E11'dir. (SUB-006 hariç SUB-001..005 E08-owned; delegation'ın MODEL-driven varyantı burada, config-driven E08'de kanıtlı.)
- **MCP/skills/hooks/remote tools** → E12. Buradaki tool yüzeyi yalnız BUILT-IN tool'lardır (`task`/`agent`/`todo`/file/shell/git); external tool extension SDK'sı E12.
- **Encrypted SecretRef backend** → E13 (LP §7.1). Credential broker short-lived token MINT eder ama ROOT GitHub App private key'i LP-0 file-secret köprüsüyle durur (E13 mühürler); broker'ın minting seam'i E13-ready ama root-key storage E13 devri.
- **SDK session/coding ergonomics** → E16. UAT harness API'yi doğrudan sürer; yeni SDK yüzeyi açılmaz.

## 3. E09 acceptance contract — canlı eşleme

Exit gate (master plan §8/E09; §63.2 pass condition): **journey 63.2 kill OLMADAN geçer; final repository SHA/diff doğru; credential engine/events/process-args/snapshot'ta YOK; duplicate tool/push/PR YOK.** Case'ler `tests/uat/cases/<ID>/case.yaml` disiplinini izler (alanlar: `id`, `name`, `proof_class`, `provider`, `input`, `expect_status` — `tests/uat/local_live_test.go:35-41`); isim = gerçekten assert edilen davranış (LP-0 follow-up dersi: overclaim yok). Proof sınıfı master plan §10.2 (`unit`/`component-real`/`e2e-deterministic`/`live-provider`/`fault-live`/`external-receipt`); E09 `external-receipt` sınıfını ilk kez kullanır (gerçek Git/PR destination receipt'i — sahte remote ile GEÇMEZ).

| Case | Kanıt | Proof class | Ev |
|---|---|---|---|
| REP-001 | preparation exact commit/tree verir + model-bağımsız preparation receipt (base commit, tree hash, branch) | e2e-deterministic (gerçek Git source) | T3 |
| REP-002 | hostile .git config/hooks/submodule → host komut/credential escape yok; hooks disabled, submodule URL validate | e2e-deterministic (security) | T3 |
| REP-003 | brokered Git credential remote-URL/env-dump/process-args/log/snapshot/model-context'te YOK | e2e-deterministic + credential-scan (exit-gate invariantı) | T3 |
| REP-004 | default/protected branch write → explicit grant olmadıkça DENY | unit + e2e-deterministic | T8 |
| REP-005 | changeset (file/patch/test/provenance) model summary'sinden BAĞIMSIZ ve eksiksiz | e2e-deterministic | T5 |
| REP-006 | approved branch push → exact commit'ler BİR kez; scoped token operasyon sonrası yok edilir | external-receipt (gerçek remote ref) | T8 |
| REP-007 | push ack kaybı → remote ref reconciliation; duplicate/force push YOK | external-receipt + fault-live | T8 |
| REP-008 | duplicate PR request/callback → tek PR + stabil external receipt | external-receipt | T8 |
| REP-009 | approval sonrası PR head SHA değişti → stale approval merge EDEMEZ, action denied | external-receipt + unit | T8 |
| REP-010 | base branch hareketi → görünür rebase/merge/wait; remote değişiklik sessizce düşmez | external-receipt + e2e-deterministic | T8 |
| REP-011 | child merge conflict → parent workspace tutarlı kalır, explicit resolution | e2e-deterministic | T6 |
| REP-012 | unsafe local bind → prominent warning + exact host scope + managed service unavailable | e2e-deterministic + unit | T1 |
| SAN-001 | file tool path traversal/symlink/device/socket → workspace dışı erişim DENY | component-real + e2e-deterministic | T4 |
| SAN-002 | host/container-runtime socket → absent veya denied | component-real | T4 |
| SAN-003 | CPU/mem/disk/process exhaustion → bounded termination, diğer tenant sağlıklı, usage recorded | component-real + fault-live | T4 |
| SAN-004 | network private/metadata scan → denied + finding/audit emit | component-real | T4 |
| SAN-005 (create yarısı) | workspace snapshot → file/index/tree checksum eşit, exclusion'lar boş/secret'sız; RESTORE E10 | component-real | T1 |
| SAN-006 (fence yarısı) | fencing ilerledikten sonra stale writer snapshot/write REDDEDİLİR; host-kill tetiği E10 | component-real + fault-live | T1 |
| SAN-010 | preview/terminal route → expired/wrong-tenant DENY, doğrudan sandbox adresi expose edilmez | component-real | T4 |
| SUB-006 | child code mutation → isolated branch/worktree + explicit conflict-aware merge; parent concurrent mutate edilmez | e2e-deterministic | T6 |
| REG-001 (authored) | durable `task` state context reset/yeni attempt sonrası DB'den okunur; AI "ne bitti/ne bitmedi"yi kaybetmeden devam eder | e2e-deterministic | T7 |
| REG-002 (authored) | durable `task`/`todo` iki client'a AYNI görünür + update'ler ordered journal'da | e2e-deterministic | T7 |
| DEL-001 (authored) | model `agent`/`spawn` tool_call → engine `child.request` emit → child terminal → parent'a typed result | live-provider (gerçek model tool_call) + e2e-deterministic | T7 |
| DEL-002 (authored) | `send_to_agent`/`agent_message` + child-idle event → parent↔child model-facing mesaj round-trip | e2e-deterministic | T7 |
| APV-001 (authored) | side-effect tool pending-approval üretir → `approve` sonraki boundary'de ilerletir, `deny` bloklar; pending yokken E08 `command.rejected` KORUNUR | e2e-deterministic | T8 |

## 4. Runtime topology delta

```text
E08 (bugün):  Session ─> Response N ─> Run ─> Attempt ─> engine loop(AWAITING_TOOLS) ─> terminal
                 │                         │  tool.request → dispatchTool → tool-broker(in-process)
                 │  commands (pump)        └─ child.request → ChildRun (inline, workspace_mode taşınır/enforce YOK)
                 └─ N client SSE attach

E09 (hedef):  Session ─> Response N ─> Run ─> Attempt ─┬─ engine loop
                 │                                     │   tool.request → dispatchTool ─┬─ file/shell → SANDBOX-BACKED exec
                 │                                     │                                └─ task/todo → durable registry (DB)
                 │                                     ├─ child.request (MODEL-driven: agent tool_call)
                 │                                     │        └─ ChildRun ─> worktree/branch (workspace_mode enforce)
                 │  commands (pump)                    └─ side-effect tool → PENDING APPROVAL ─> approve/deny command
                 │   approve/deny ──────────────────────────────┘
                 │
    Workspace(logical) ─> Binding ─> Allocation(fencing) ─> single-writer lease ─> OCI volume mount ─> /workspace
                 │                                                                        │
    Repository ─> clone@exact-commit ─> credential broker(mint→use→revoke) ────────────┘
    Changeset ─> patch/test artifacts ─> Artifact store (S3/SeaweedFS: PUT row+bytes+checksum)  ── retention: gerçek byte delete
    Publication ─> push branch / draft PR ─> external Git receipt (idempotent, reconciled)
```

- **Yeni frame yok; mevcut round-trip genişler.** `tool.request`/`tool.result` zaten var; E09 yeni `Tool` değerleri kaydeder ve `dispatchTool`'un arkasına sandbox-backed seam koyar. `child.request` schema'sına `workspace_mode` DECLARE edilir (bugün Go/Py-only) ve şema-pin testine girer (LP Task 9 kalıbı). Yeni event tipleri: `workspace.*`, `artifact.*`, `changeset.*`, `approval.*`, `push.*`, `pull_request.*`, `task.*` — her biri `event-types.json`'a eklenir + `make generate` + drift gate.
- **`oci.ContainerSpec` volume alanı kazanır** (`adapters/sandboxes/oci/driver.go`); `docker.go` mount eder; `packages/runner/supervisor.go:173` engine container'ı workspace allocation'ı mount ederek açar. Engine credential/S3 GÖRMEZ (§24 topolojisi korunur).
- **Artifact store** (LP §7.2): control-plane'de tek S3 boundary (`apps/control-plane/internal/artifacts/`); SeaweedFS compose'da hazır (`deploy/compose/compose.yaml:32`, port 8333), `aws-sdk-go-v2` go.mod'da (`:8-9`) — bugün SIFIR Go çağrısı. `artifacts` tablosu şemada var (`storage/migrations/000001_core.up.sql:288`) ama yazarsız; retention Reaper yalnız response purge ediyor (`retention.go:26`) — artifact-byte delete YOK.
- **Migration'lar `000008`'den başlar** (main'de en yüksek `000007_child_runs`; phase-08 planındaki "000006" off-by-one, düzeltme kaydı). Task başına küçük + idempotent-re-run testli (repo kalıbı 000002 retention).
- Dependency direction korunur: state-machines pure; api → execution → coordinator/storage; engine DB/S3/credential görmez; credential broker + artifact store yalnız control-plane.

## 5. Task plan

### Task 1: Workspace foundation — Logical Workspace/Binding/Allocation, single-writer lease, sandbox mount, unsafe local bind

**Files:** Create `storage/migrations/000008_workspaces.up.sql`/`.down.sql`, `packages/state-machines/workspace.go` (WorkspaceBinding SM), `adapters/sandboxes/oci/workspace/allocation.go` (logical→fiziksel allocation + fencing), `apps/control-plane/internal/execution/workspace.go` (allocation + single-writer lease + snapshot create); Modify `adapters/sandboxes/oci/driver.go` (`ContainerSpec`'e `Volumes`/`Mounts` alanı — bugün yok, `:36-43`), `adapters/sandboxes/oci/docker.go` (mount), `packages/runner/supervisor.go:173` (engine container workspace mount), `storage/queries/workspaces.sql`, contract schema (`workspace_mode` değerleri: `none`/`read_only`/`isolated`) + `make generate`.

Semantik (§29.7-29.9): Workspace = process/host-bağımsız **logical filesystem lineage**; WorkspaceBinding onu tek session/run'a bağlar (`requested→provisioning→preparing→ready→leased`, yan durumlar `snapshotting/paused/restoring/host_lost→recovering→failed`, `→destroying→destroyed`). Logical workspace id host-hareketinde SABİT; her fiziksel allocation ayrı allocation id + **fencing token** taşır (§29.7 son madde). **Single-writer lease (§29.8):** mutable workspace'te en fazla bir aktif writer lease; root run normalde sahiptir; child'lar read-only snapshot / isolated COW branch alır (enforce T6). Client terminal attach aynı serialized writer boundary'yi paylaşır. Logical yollar §29.9: `/workspace/repo`, `/workspace/scratch`, `/workspace/artifacts`, `/runtime`, `/secrets` normalde YOK. **SAN-006 fence-yarısı:** fencing ilerledikten sonra stale allocation authoritative snapshot upload EDEMEZ (§29.8 line 3070) — E09 fence-advance'i deterministik/component-real tetikler, gerçek host-kill E10. **SAN-005 create-yarısı:** snapshot create → file/index/tree checksum + exclusion set (secret yok); RESTORE E10. **Unsafe local bind (§30.13, REP-012):** default snapshot/copy; direct mutable bind mount yalnız explicit unsafe-development flag'iyle, prominent warning + "sandbox isolation iddia edilemez" + publication disabled. Workspace state hiçbir session/repo/publication permission vermez (§29.9 line 3071).

- [ ] Failing testler: `TestWorkspaceBindingTransitions` (SM pure; registry-sync + property, E02 kalıbı); `TestAllocationCarriesFencingTokenStableLogicalID` (host-hareketi simülasyonu: logical id sabit, allocation id + fence değişir); `TestSingleWriterLeaseRejectsSecondWriter` (SQLSTATE-level assert — LP Task 3 reviewer kalıbı); `TestFencedStaleWriterSnapshotRejected` (SAN-006 fence yarısı: fence ilerler → eski allocation snapshot/write DB seviyesinde reddedilir); `TestSnapshotCreateChecksumsAndExclusions` (SAN-005 create yarısı: checksum eşit, `/secrets` exclusion boş); `TestEngineContainerMountsWorkspaceVolume` (component-real: OCI container `/workspace` görür, yazılan dosya allocation'da kalır); `TestUnsafeLocalBindRequiresFlagWarnsDisablesPublication` (REP-012); migration idempotent re-run + down.
- [ ] Fail'i doğrula; minimum implementasyon; `ContainerSpec` volume alanı + docker mount + supervisor mount; `workspace_mode` değer enum'ı schema'ya declare edilir (enforce T6, ama şema-pin burada).
- [ ] **LIVE smoke:** gerçek provider (`provider-one`) run'ı gerçek workspace allocation'a bind edilir; engine container gerçek `/workspace` mount'unu görür; forced file-write tool'u (T4'ün minimum stub'ı burada gerekmez — smoke: bir sabit dosya seed + engine'in görmesi) allocation'da kalır. Honest ceiling: bu smoke **gerçek mount**'u kanıtlar, model davranışını değil (model henüz tool çağırmıyor — o T4).

**Verify:** `make verify && make test-component && make test-e2e && make test-live-provider PROVIDER=provider-one` (workspace mount smoke); generated drift 0.

### Task 2: Object-store consumption / artifact write-path (LP planı §7.2 ZORUNLU carve-out)

**Files:** Create `apps/control-plane/internal/artifacts/store.go` (S3-compatible boundary; `aws-sdk-go-v2` zaten `go.mod:8-9`), `apps/control-plane/internal/artifacts/writer.go`; Modify `apps/control-plane/internal/execution/retention.go` (Task 11d'nin bugün YOK olan "artifact byte'larını sil" adımı gerçek delete olur — Reaper `:26` şu an yalnız `PurgeExpiredStoreFalse` response'lara), `apps/control-plane/cmd/palai-control-plane/main.go` (S3 client wiring: endpoint/bucket env — compose SeaweedFS `:8333`), `storage/queries/artifacts.sql`.

Semantik (LP §7.2 + §22.6): LP-0'da SeaweedFS ADR-0004 digest'iyle sağlıklı çalışır, doctor S3 ping yapar, ama control-plane'de TEK S3 çağrısı yoktur — `artifacts` tablosu (`000001_core.up.sql:288`: id/org/project/run_id/object_key/size_bytes/checksum) yazarsızdır. İlk gerçek artifact üreticisi E09'un patch/test artifacts'ıdır; S3 client buraya, write-path ile BİRLİKTE gelir. Artifact = immutable versioned output (media type/size/checksum, logical type: report/patch/diff/log/test-result/…, malware-scan status, provenance links — §22.6). Engine/runner S3 credential GÖRMEZ (§24 — yalnız control-plane). Bu task changeset (T5) için önkoşuldur (patch/test artifacts oraya yazılır).

- [ ] Failing testler (gerçek SeaweedFS'e karşı component test — LP §7.2 TDD çerçevesi birebir): `TestArtifactPutRecordsRowAndBytes` (write → `artifacts` satırı + S3 object + checksum eşleşir); `TestArtifactReadIsTenantScoped` (başka tenant'ın object_key'i 404, existence disclosure yok — LP Task 5 kalıbı); `TestStoreFalsePurgeDeletesArtifactBytes` (Task 11d Reaper'ı store:false run'ın artifact byte'larını S3'ten GERÇEKTEN siler — bugünkü absent adımın kapanışı, response purge'üne paralel).
- [ ] Fail'i doğrula; minimum put/get/delete boundary; doctor `object_store` check'i DEĞİŞMEZ (ping zaten var).
- [ ] **LIVE smoke:** gerçek provider run'ı bir shell/file tool çıktısını (T4 henüz yoksa: run terminal output'unu) artifact olarak yazar; gerçek SeaweedFS'te row+bytes+checksum doğrulanır. Honest ceiling: bu component-real infra smoke'u; "live" öğe gerçek run'ın gerçek S3'e byte yazmasıdır (model reasoning'i değil).

**Verify:** `make verify && make test-component TEST=artifacts` PASS; `make test-fault CASE=coordinator` regress yok (retention değişimi).

### Task 3: Deterministic clone + untrusted-repo policy + scoped GitHub App credential broker

**Files:** Create `adapters/repositories/prepare.go` (§30.3 11-adım preparation), `adapters/repositories/policy.go` (§30.4 untrusted-repo defenses), `adapters/repositories/broker.go` (§30.2/§28.11 scoped credential broker — LP Task 10 `SecretResolver` seam'i mint noktasına takılır), `storage/migrations/000009_repository_bindings.up.sql`/`.down.sql` (§30.1 binding: submodule/LFS/commit-signing/branch/path/PR-target policy + preparation receipt), `storage/queries/repository_bindings.sql`; Modify `apps/control-plane/internal/execution/` (run.start'a repo preparation adımı — workspace `ready` olmadan clone yok), contract schema (`repository_binding` + preparation receipt) + `make generate`.

Semantik (§30.1-30.4): Preparation **infrastructure-owned**, 11 adım: ref resolve → read credential MINT → clean repo init (safe Git config) → exact commit/ref fetch (bounded history) → policy gerektirirse commit identity verify → work branch checkout / detached read-only → allowed submodule/LFS materialize → **hooks + unsafe filter'lar default DISABLED** → credential REMOVE → base commit/tree hash/branch/preparation receipt RECORD → engine'e expose. **Initial provenance model davranışına bağlı DEĞİL** (§30.3 line 3273 — REP-001). Untrusted-repo (§30.4, REP-002): repo `.git` config sistem credential helper'ları override edemez / arbitrary komut çalıştıramaz; hooks disabled; submodule URL validate + ayrı-scope'lu credential; symlink/case-collision check; archive/LFS size-limit; repo instruction'ları untrusted etiketli; committed-secret detect. **Credential broker (§30.2/§28.11, REP-003 — exit-gate invariantı):** short-lived repository-scoped, read/push/PR/checks/merge için AYRI scope, yalnız credential helper / brokered Git op'una girer, **argv/env-dump/remote-URL/log/snapshot/model-context'ten HARİÇ**, operasyon sonrası revoke/expire; GitHub App installation token > user PAT; **model asla raw Git credential görmez** (§30.2 line 3255). Broker token audience-bound + one-operation (§28.11): org/project/run/attempt-fence/tool-call'a bağlı, dakikalar içinde expire, başka tool/destination kullanamaz.

- [ ] Failing testler: `TestPrepareYieldsExactCommitWithReceipt` (REP-001; base commit/tree hash/branch receipt, model-bağımsız — gerçek local Git remote); `TestHostileRepoConfigHooksSubmoduleContained` (REP-002; crafted hostile-repo fixture: hook çalışmaz, submodule URL reddedilir, .git config override edemez); `TestBrokeredCredentialAbsentFromAllSurfaces` (REP-003; token remote-URL/env/argv/log/snapshot/engine-frame taramasında YOK — hatta FAKE token bile absent olmalı, absence deterministik kanıtlanır, `tests/security/repository/` altında — LP-010 secret-scan kalıbı); `TestReadCredentialRevokedAfterPreparation`; migration idempotent + down.
- [ ] Fail'i doğrula; minimum preparation + policy + broker; root GitHub App private key LP-0 file-secret köprüsüyle gelir (E13 mühürler — §7 devri), broker yalnız short-lived token MINT eder.
- [ ] **LIVE smoke:** gerçek provider run'ı gerçek Git repo'yu (gated: `PALAI_GIT_REPO` test repo; live tier'da gerçek GitHub App installation) exact commit'te clone eder; `tests/security/repository/` credential-scan gerçek installation token'ın engine/events/argv/snapshot'ta olmadığını doğrular. Honest ceiling: bu REP-003 exit-gate invariantının canlı doğrulaması; deterministik tier absence'ı fake token ile de kanıtlar (absence proof provider-realness gerektirmez), live tier gerçek token ile teyit eder.

**Verify:** `make verify && make test-component TEST=repository && make test-security && make test-e2e`; hygiene grep (Git PAT + App key + installation token literalleri) = 0.

### Task 4: Sandbox-backed file/shell tools + sandbox isolation + preview/terminal auth

**Files:** Create `packages/tool-broker/sandbox_exec.go` (in-process broker'ın arkasına sandbox-backed execution seam — `broker.go:83` in-process, `:1` "pure conformance"; workspace-touching tool'lar bu seam'den geçer), `adapters/sandboxes/oci/workspace/exec.go` (workspace-scoped file + shell exec), `apps/control-plane/internal/execution/tools/file.go` + `tools/shell.go` (§28.7/§28.8 tool tanımları), `apps/control-plane/internal/execution/preview_auth.go` (§29.16), `tests/security/repository/` (SAN suite); Modify `apps/control-plane/cmd/palai-control-plane/main.go:93` (`ConformanceMathAdd` yanına file/shell `Tool` değerleri kaydedilir), `adapters/sandboxes/oci/driver.go` (`Limits`: CPU/mem/disk/process — SAN-003), egress policy (SAN-004).

Semantik (§28.7-28.8, §29.16): **File tool (§28.7, SAN-001):** her path workspace root'a göre resolve, traversal/host-mount/device/socket/disallowed-symlink escape REDDEDİLİR; bounded read/write/patch/list/search/stat/checksum; atomic write; before/after hash + changed path RAPOR (changeset event — T5 tüketir); binary artifact üstünden (T2), model text'te değil; read-only/protected-path policy; likely-secret read blocked/redacted. **Shell tool (§28.8, SAN-002):** default **argv-form** (shell string değil), pipeline/redirection için explicit shell mode; sandbox user + workspace'te; timeout/process-count/CPU/mem/disk/output limit; bounded stdout/stderr stream + full log artifact (T2); cancel'da **process-group kill**; secret redaction; executable resolution + exit/signal record; **container runtime / host control socket ÇAĞIRAMAZ**. External side-effect / protected-path komutu approval gerektirebilir (§28.8 line 2666 — bu bir pending-approval kaynağı, T8'e köprü). **SAN-003:** CPU/mem/disk/process exhaustion → bounded termination, diğer tenant sağlıklı, usage RECORDED. **SAN-004:** private/metadata egress → denied + finding/audit. **SAN-010 (§29.16):** preview/terminal route expired/wrong-tenant DENY, doğrudan sandbox adresi expose edilmez.

**Pivot notu (E08 memory ceiling'inin düşüşü):** E08'de real run'lar single-step'ti ve tool yüzeyi real provider'a açık DEĞİLDİ (yalnız in-process math-add). Round-trip plumbing zaten tam (`loop.py AWAITING_TOOLS` ↔ `orchestrator.go:263` ↔ provider-one `tools`+`ForceToolCall`); E09 gerçek coding tool'larını kaydedip real provider'ın İLK gerçek multi-step tool loop'unu koşar. Real provider free-choice tool-calling GÜVENİLİRLİĞİ bir model-davranışı sorusudur, Palai-correctness'i değil — bu yüzden LIVE smoke `tool_choice:required` (`ForceToolCall`) ile round-trip'i DETERMINISTIK zorlar ve **round-trip'i** kanıtlar, modelin spontane tool-seçimini değil (E08 T6 "live steer structurally unobservable" honest-ceiling disiplini).

- [ ] Failing testler: `TestFileToolDeniesWorkspaceEscape` (SAN-001; `../`/symlink/`/dev`/`/proc`/socket hepsi denied, component-real gerçek sandbox FS); `TestFileWriteReportsBeforeAfterHashAndChangedPaths` (changeset event kaynağı); `TestShellToolArgvFormSandboxUserLimitsProcessGroupKill` (SAN-002/§28.8; runtime socket erişimi denied); `TestResourceExhaustionBoundedTerminationUsageRecorded` (SAN-003; fault-live: gerçek cgroup limit + termination, komşu tenant intact, usage row); `TestEgressDeniesPrivateAndMetadataWithFinding` (SAN-004; gerçek egress proxy, `169.254.169.254` denied + finding); `TestPreviewRouteDeniesExpiredAndWrongTenant` (SAN-010); `TestShellSecretRedactedInDisplayAndOutput`.
- [ ] Fail'i doğrula; minimum sandbox-backed seam + file/shell tool; engine round-trip DEĞİŞMEZ (mevcut `dispatchTool` yolu genişler).
- [ ] **LIVE smoke (T4 pivotu — E09'un en bulgu-eğilimli smoke'u):** gerçek `provider-one` run'ı `tool_choice:required` ile file tool'unu ÇAĞIRIR (workspace'te dosya yazar), sonra shell tool'uyla test koşar; engine gerçek model→tool→model round-trip'ini tamamlar; workspace mutate olur. Bu, real provider'ın İLK gerçek coding tool-use'u — E08 single-step ceiling'i burada düşer. Findings (varsa) LP disiplini gibi kayıt altına alınır.

**Verify:** `make verify && make test-component && make test-security && make test-e2e && make test-live-provider PROVIDER=provider-one` (forced tool round-trip); `make test-fault CASE=sandbox` (SAN-003, yeni fault dizini Makefile'a bağlanır).

### Task 5: Changeset + patch/test artifacts + secret scan

**Files:** Create `storage/migrations/000010_changesets.up.sql`/`.down.sql` (immutable changeset + findings), `apps/control-plane/internal/execution/changeset.go` (file-tool write event'lerinden derlenen first-class summary), `apps/control-plane/internal/execution/tools/commit.go` (§30.7 commit tool), `storage/queries/changesets.sql`; Modify `apps/control-plane/internal/execution/tools/file.go` (write → changeset accrual), contract schema (`changeset.*` event tipleri) + `make generate`.

Semantik (§30.6-30.7): **Changeset first-class immutable summary:** base + final commit/tree; added/modified/deleted/renamed/binary files; **patch artifact** + truncation marker (T2'ye yazılır); generated files + excluded paths; **tests/checks + evidence** (test-log artifact); commit list; authoring run/tool lineage; policy + approval state; **possible secret/license findings**. **Final model response changeset'in YERİNE GEÇMEZ** (§30.6 line 3314 — REP-005; changeset model summary'sinden BAĞIMSIZ derlenir, file-tool event ledger'ından). Commit tool (§30.7): deterministic configured author identity, credential GEREKMEZ, opsiyonel brokered signing (key workspace'e girmez), **commit push permission imply ETMEZ**. Secret scan: changeset'e giren dosyalarda likely-committed-secret finding (§30.4 detect ile aynı tarayıcı, `tests/security/` seam'i).

- [ ] Failing testler: `TestChangesetCompleteIndependentOfModelSummary` (REP-005; changeset file/patch/test/provenance eksiksiz, model prose'u değiştirilse bile changeset sabit — file-tool event'lerinden derlenir); `TestPatchArtifactWrittenToObjectStore` (T2 tüketimi: patch/test-log artifact row+bytes); `TestCommitDoesNotImplyPush` (§30.7; commit tool push capability üretmez); `TestChangesetFlagsSecretFinding` (secret scan changeset'te finding üretir); migration idempotent + down.
- [ ] Fail'i doğrula; minimum changeset accrual + commit tool; changeset immutable (content-addressed — LP Task 11 content_hash kalıbı).
- [ ] **LIVE smoke:** gerçek provider run'ı file tool'uyla kod düzenler → changeset otomatik derlenir → patch artifact gerçek S3'te → model'in prose özeti değiştirilse bile changeset diff'i gerçek dosya değişimini taşır. Honest ceiling: REP-005'in load-bearing "model-bağımsız provenance" invariantı; live tier gerçek edit'le, deterministik tier fake-driven edit'le kanıtlar.

**Verify:** `make verify && make test-component && make test-e2e && make test-live-provider PROVIDER=provider-one`.

### Task 6: Child branch/worktree + explicit conflict-aware merge + workspace_mode enforcement

**Files:** Create `adapters/sandboxes/oci/workspace/worktree.go` (isolated branch/worktree + COW), `apps/control-plane/internal/execution/tools/merge.go` (§30.5 explicit conflict-aware merge tool), `storage/migrations/000011_worktree_merge.up.sql`/`.down.sql` (worktree + merge record; child run zaten `parent_run_id` taşıyor, `000007`); Modify `apps/control-plane/internal/execution/child_dispatch.go` (`workspace_mode` ARTIK enforce: `none`/`read_only`/`isolated` dallanır — `:137` "enforced by E09" notu kapanır), `engines/reference/src/palai_engine/loop.py:170` (workspace_mode child.request'te taşınır, schema-pin T1'de declare edildi), contract schema-pin testi.

Semantik (§30.5, §25.18, §12): Default mutable iş **`agent/<session-short-id>/<run-short-id>`** generated branch; policy prefix/source/force-push/protected/fork kontrol eder; **protected/default branch'te direct iş default DENY**. **Child agents (§30.5 line 3298, SUB-006):** kod düzenleyen child'lar **isolated worktree/branch** kullanır; parent worktree'ye merge **explicit tool call**'dur — conflict DETECT eder + source child run RECORD eder. `workspace_mode` enforcement (E08 devri kapanır): `read_only` → child read-only snapshot alır (yazamaz); `isolated` → ayrı COW branch/worktree; `none` → workspace yok (E08 davranışı). **Parent workspace concurrent mutate edilmez** (§12 line 374); merge stratejisi explicit. Child secret inherit ETMEZ (E08 invariantı korunur). **REP-011:** child merge conflict → parent workspace TUTARLI kalır, explicit resolution (merge tool conflict'i döndürür, sessizce overwrite etmez).

- [ ] Failing testler: `TestChildWorkspaceModeReadOnlyDeniesWrite` (workspace_mode enforcement: `read_only` child file-write tool'u denied); `TestChildIsolatedWorktreeSeparateBranch` (SUB-006; `isolated` child ayrı `agent/…` branch/worktree, parent'a yazmaz); `TestExplicitMergeDetectsConflictParentConsistent` (REP-011; conflict → merge tool typed conflict result, parent worktree değişmez, source child run kaydedilir); `TestProtectedBranchDirectWorkDenied`; `TestWorkspaceModeSchemaPinned` (child.request schema'sında `workspace_mode` enum'ı — sessiz shape drift yasak); migration idempotent + down.
- [ ] Fail'i doğrula; minimum worktree + merge tool + workspace_mode dallanması; recursive delegation OFF kalır (E08 `maxChildDepth=1`; bu task depth'i artırmaz).
- [ ] **LIVE smoke:** gerçek provider run'ı config-driven bir child (E08 delegation yolu) `isolated` workspace_mode ile spawn eder; child gerçek worktree'de kod düzenler; explicit merge parent'a conflict-aware uygulanır. Honest ceiling: child'ın workspace enforcement'ı canlı kanıtlanır; MODEL-driven spawn (agent tool_call) T7'de.

**Verify:** `make verify && make test-e2e && make test-component && make test-fault CASE=subagents && make test-live-provider PROVIDER=provider-one`.

### Task 7: Model-facing tool yüzeyi — durable task/agent/todo registry + model-driven delegation + parent↔child mesajlaşma

**Files:** Create `storage/migrations/000012_durable_tasks.up.sql`/`.down.sql` (`tasks`/`todos` — session-scoped durable primitive'ler, session/run/command gibi), `apps/control-plane/internal/execution/tools/task.go` + `tools/todo.go` + `tools/agent.go` (model-facing tool'lar), `apps/control-plane/internal/execution/child_message.go` (parent↔child mesaj + child-idle event), `storage/queries/tasks.sql`; Modify `apps/control-plane/cmd/palai-control-plane/main.go:93` (task/todo/agent tool'ları kaydedilir), `engines/reference/src/palai_engine/loop.py:153` (`_request_children` artık MODEL tool_call'ından tetiklenir — E08'de config-driven `delegations` idi), contract schema (`task.*` event tipleri, agent tool schema) + `make generate`.

Semantik (master plan line 410-411, §11/§25.18-19): **Durable tool registry** — `task`/`agent`/`todo` + custom, model-facing tool yüzeyinde: **DB-backed, session-scoped, DURABLE task state**, multi-client görünür (E08 ordered journal + attach), **context-recovery** (AI "ne bitti/ne bitmedi"yi DB'den okur; context dolsa/reset olsa bile uzun kodlamaya kaldığı yerden devam eder), task'a ek metadata girilebilir. `agent` tool E08 T5 ChildRun (`child.request` frame) altyapısını **model-facing** yapar; `task`/`todo` YENİ durable primitive'ler (session/run/command kalıbında; §12 "durable tool-call completion is keyed by stable `tool_call_id`" line 378 çıpası). Bu Palai'nin "API'den durable ajan çalıştıran platform" diferansiyatörü — Claude Agent SDK'nin ephemeral Todo'sunun durable/observable/resumable hâli. **Model-driven delegation:** model `agent`/`spawn` tool_call yapar → engine `child.request` EMIT eder (E08 T5'te delegation raw-body `delegations` ile config-driven idi; E09 tool surface bunu MODEL-driven yapar, AYNI ChildRun'a düşer — `admitChild` bounds değişmez). Child kod düzenlerse T6 worktree'sini kullanır. **Parent↔child model-facing yarısı:** `send_to_agent`/`agent_message` tool + child-idle event → parent model (§25.18 "child output enters parent context as typed result, not hidden transcript"). Durable child-detach yarısı E10.

- [ ] Failing testler: `TestDurableTaskSurvivesContextReset` (REG-001 — DİFERANSİYATÖR: task'lar oluşturulur, bir kısmı done işaretlenir, context reset / yeni attempt simüle edilir, AI task state'ini DB'den okur — done/not-done INTACT, kaldığı yerden devam); `TestDurableTasksMultiClientVisible` (REG-002; E08 çok-client attach harness'i yeniden kullanılır, iki client aynı task listesi + ordered update'ler); `TestModelAgentToolCallEmitsChildRequest` (DEL-001 deterministic yarısı: fake adapter `agent` tool_call üretir → `child.request` emit → child terminal → parent journal'ında typed result + child_run_id); `TestParentChildModelFacingMessageRoundtrip` (DEL-002; `send_to_agent` → child görür, child-idle event → parent model `agent_message` alır); `TestTodoToolPersistsAndRecovers`; migration idempotent + down.
- [ ] Fail'i doğrula; minimum task/todo/agent tool + child-message; `admitChild` bounds (`maxChildDepth=1`, `maxChildFanout=4`) DEĞİŞMEZ; task/todo session-scoped, run terminal'inde silinmez (durable).
- [ ] **LIVE smoke (DEL-001 live yarısı):** gerçek `provider-one` run'ı `tool_choice:required` ile `agent` tool_call yapar → gerçek `child.request` → child gerçek provider'a gider (SUB-002 kalıbı: farklı/ucuz model-id, iki farklı gerçek provider request id) → typed result parent'a döner; ayrıca gerçek run `task` tool'uyla durable task yazar. Honest ceiling: DEL-001 modelin gerçek `agent` tool_call'ını forced-tool ile kanıtlar; free-choice delegation reliability model-davranışıdır, round-trip kanıtlanır.

**Verify:** `make verify && make test-e2e && make test-component && (cd engines/reference && pytest) && make test-live-provider PROVIDER=provider-one`.

### Task 8: Publication — push branch + draft PR + approve/deny (pending-approval kaynağı) + idempotency/reconciliation

**Files:** Create `adapters/repositories/publish.go` (§30.8-30.12 push/PR/reconcile), `apps/control-plane/internal/execution/approval.go` (pending-approval kaynağı + boundary delivery — `command_pump.go` kalıbının approval eşleniği), `apps/control-plane/internal/execution/tools/push.go` + `tools/pull_request.go`, `storage/migrations/000013_approvals_publications.up.sql`/`.down.sql` (pending approvals + push receipts + PR records + idempotency keys); Modify `packages/coordinator/commands.go:194` (approve/deny artık `no_pending_approval` switch'ini pending-approval kaynağına danışır — pending varsa `nil`/queued, yoksa E08 reject KORUNUR), `apps/control-plane/internal/execution/command_pump.go` (approval boundary pump), contract schema (`approval.*`/`push.*`/`pull_request.*`) + `make generate`.

Semantik (§30.8-30.12, §22.4-22.5): **Publication decompose (§30.8):** `push_branch`/`open_pull_request`/`update_pull_request`/`comment`/`set_status`/`merge`/`create_release` — her biri BAĞIMSIZ capability/approval/credential/idempotency-key/audit. Default stable policy: agent branch'e push + draft PR (approval sonrası) İZİNLİ; merge/release/protected-branch-push DENY. **Push (§30.9, REP-006/007):** target refresh + divergence detect → branch policy re-eval → approval gerekiyorsa **exact commit/diff GÖSTER** → write-only credential MINT → **force DISABLED** push → provider receipt + remote commit PERSIST → credential REMOVE → **ambiguous network completion'ı remote ref sorgulayarak RECONCILE** (retry'dan önce). Force push ayrı high-risk capability, asla infer edilmez. **PR (§30.10, REP-008):** exact head/base; title/body model-proposed policy-filtered; **idempotency run/branch için mevcut PR'ı yaratmadan ÖNCE bulur**; duplicate callback duplicate PR YARATMAZ; PR id/URL structured result. **Merge (§30.11, REP-009):** ordinary coding set'ten HARİÇ; enable'sa exact head SHA approval'a bağlı, **stale approval değişmiş head'i merge EDEMEZ**. **Race (§30.12, REP-010):** base hareketi run'ın recorded base'ini mutate etmez; policy fail/rebase/merge/PR seçer; **model remote değişikliği sessizce discard EDEMEZ**. **Approve/deny (§22.4-22.5, APV-001 — E08 devrinin kapanışı):** side-effect tool (push/PR) pending-approval ÜRETİR (exact tool+version, redacted args, predicted capabilities, repo diff/command/destination, expiry, allowed approver, **request-hash'e bağlı one-shot token**); `approve_once`/`approve_for_run`/`deny`/`expire`; **arg edit yeni tool call + yeni approval**; model prose exact operation display'in yerine GEÇMEZ. E08 `TestApproveWithoutPendingApprovalRejected` KORUNUR (pending yokken hâlâ reject).

- [ ] Failing testler: `TestProtectedDefaultBranchWriteDenied` (REP-004; explicit grant olmadıkça DENY); `TestApprovedPushExactCommitsOnceTokenDestroyed` (REP-006; external-receipt: gerçek local bare repo remote ref, tek push, credential removed); `TestLostPushAckReconcilesNoDuplicateForce` (REP-007; external-receipt + fault-live: ack kaybı → remote ref query → duplicate/force yok); `TestDuplicatePRRequestCallbackSinglePR` (REP-008; external-receipt: idempotency mevcut PR bulur); `TestStaleApprovalCannotMergeChangedHead` (REP-009; head SHA değişti → approval invalid → deny); `TestBaseMovementNoDroppedRemoteChanges` (REP-010); `TestPendingApprovalApproveProceedsDenyBlocks` (APV-001; side-effect tool pending üretir, approve boundary'de ilerletir, deny bloklar; pending-yok E08 reject korunur); migration idempotent + down.
- [ ] Fail'i doğrula; minimum push/PR/reconcile + approval kaynağı + command switch bağlama; write credential broker T3'ten (ayrı push-scope token).
- [ ] **LIVE smoke:** gerçek provider run'ı kod düzenler → changeset → `push` tool pending-approval üretir → `approve` command → gerçek Git remote'a (gated: gerçek GitHub App installation + test repo, live tier prerequisite; deterministik tier faithful double) tek push + draft PR; duplicate callback tek PR; credential-scan token'ın hiçbir yüzeyde olmadığını teyit eder. Honest ceiling: PR external-receipt gerçek GitHub test repo gerektirir (provider gibi gated); push external-receipt gerçek bare repo ref'iyle CI'da da koşar.

**Verify:** `make verify && make test-component && make test-e2e && make test-fault CASE=coordinator && make test-security && make test-live-provider PROVIDER=provider-one`; hygiene grep = 0.

### Task 9: Canlı coding journey (63.2 kill'siz) + REP/SAN/SUB UAT materialization + evidence release → E09 kapanışı

**Files:** Create `tests/e2e/coding/` (coding journey harness — E08 `newStack`/`runCase` yeniden kullanılır, greenfield değil), `tests/uat/cases/REP-*/case.yaml` + `SAN-*` + `SUB-006` + `REG-*`/`DEL-*`/`APV-*` (bu dilimde kanıtlanan case'ler; isimler assert edileni söyler; §64 katalog reconciliation §7 devri), yeni evidence release `coding-0.1.0`; Modify `tests/uat/` harness + `tests/uat/evidence.go` (`external-receipt` proof-class doğrulaması — gerçek remote ref/PR receipt kuralı, `live-provider`'ın `^chatcmpl-` kuralına paralel, b20627c kalıbı), `Makefile` (`uat-coding` hedefi; `uat-local-live` + `uat-interactive` DOKUNULMAZ), `scripts/uat/` gerekiyorsa.

- [ ] Failing testler (deterministic tier): `TestCodingJourneyDeterministic` (63.2 adım 1-7,10-11 fake adapter'la scripted tool_call'larla: bind → workspace prepare@commit → file/shell edit → changeset → child (isolated worktree) → push+PR idempotent; kill YOK); her REP/SAN/SUB/REG/DEL/APV case'i kendi proof-class'ında (`case.yaml` loader `tests/uat/cases/*` sıralı okur).
- [ ] Canlı journey (`live-provider` + `external-receipt`, compose stack, gerçek OpenAI + gerçek Git destination; LP Task 15 hygiene disiplini: `.env.local`, `set -a`, credential asla argv/log/evidence): create session + **repository binding** → **workspace prepare @exact commit** (preparation receipt) → agent **file tool ile kod inceler/düzenler + shell tool ile test koşar** (gerçek tool round-trip, `ForceToolCall`) → ayrı client'tan **stream gözlem** (E08 SES-001) → **queue/steer** (E08; deterministic-tier, single-step canlı-unobservable notu E08 T6'dan devralınır) → **model switch** (aynı provider, farklı model-id) → **required research child ucuz route** (E08 SUB-002 + MODEL-driven agent tool_call, T7) → **changeset + test evidence göster** → **approved branch push + draft PR BİR kez** (external receipt). **KILL YOK** (adım 8-9 E10). Evidence manifest: preparation receipt, provider request id'ler, child run linki + route, changeset content-hash + patch artifact key, push remote SHA receipt, PR id/URL, iki client journal hash, **credential-absence scan sonucu (engine/events/argv/snapshot)**.
- [ ] Evidence verifier: `coding-0.1.0` için `VerifyRelease` + `external-receipt` kuralı (gerçek remote/PR receipt shape) çalışır; secret finding 0 (`sk-` + Git PAT + App key + installation token).
- [ ] `make uat-coding PROVIDER=provider-one` PASS (gerçek Git destination gated: `PALAI_GIT_REPO` + GitHub App installation); hygiene grep = 0; deterministik tier aynı journey'i fake adapter + faithful Git double ile CI'da koşar, CANLI tier onsuz GEÇMEZ.

**Verify:** `make verify && make test-e2e && make uat-coding PROVIDER=provider-one && make evidence-verify RELEASE=coding-0.1.0`.

## 6. Final release check (E09 exit gate)

- [ ] `make verify`, `make test-component`, `make test-e2e`, `make test-fault`, `make test-security` PASS.
- [ ] `make uat-coding PROVIDER=provider-one` bütün bu-dilim case'leri PASS; hiçbir `live-provider` case fake ile, hiçbir `external-receipt` sahte remote ile geçmedi.
- [ ] `make evidence-verify RELEASE=coding-0.1.0` PASS; secret finding 0 (`sk-` + Git PAT + GitHub App private key + installation token).
- [ ] **Exit gate cümlesi kanıtlı (§63.2):** journey 63.2 kill OLMADAN geçer; final repository SHA/diff doğru; **credential engine/events/process-args/snapshot'ta YOK** (T3 REP-003 + T9 canlı scan); **duplicate tool/push/PR YOK** (T8 idempotency/reconciliation + T9 canlı).
- [ ] Regresyon yok: `make uat-local-live PROVIDER=provider-one` (LP-0) ve `make uat-interactive PROVIDER=provider-one` (E08) hâlâ PASS; workspace/tool/publication değişiklikleri single-shot ve interactive yolları bozmadı.
- [ ] `git status --short` temiz; generated drift 0 (`make generate` sonrası diff yok — yeni event tipleri + workspace_mode schema + tool schema pinlenmiş).

## 7. Bu plana girmeyen devirler (kayıt yeri: bu commit'li plan)

1. **journey 63.2 kill+recovery yarısı + SAN-005 restore + SAN-006 host-kill + SAN-007/008 (ev: E10, `phase-10-recovery-replay.md`).** E09 fencing/single-writer-lease + snapshot CREATE + create-side checksum/exclusion'ı kanıtlar; workspace RESTORE, checkpoint/replay ladder, gerçek host-kill fault'u E10'dur. SAN-005/006 E08'in SES-009/010 checkpoint yarısıyla aynı SHARED-case disiplininde: E09 command/state yarısı, E10 recovery yarısı. RecoveryProof olmadan "recovery çalışıyor" iddiası verilmez.
2. **Parent-detached durable child + parent-child agent conversation'ın DURABLE yarısı (ev: E10, master plan line 431).** E09 model-facing yarısını (agent tool → child.request, send_to_agent, child-idle event, T7) lands; child `waiting` idle + `run.restore` ile parent-detached long-lived + durable-conversation birleşimi E10. Primitive'ler T6/T7'de kanıtlı, detach + durable-conversation E10.
3. **DB-backed model routing + LiteLLM model ADAPTER (ev: E06, `phase-06-reference-execution.md`; LP §7.3).** Journey adım 7 "ucuz route" E08 `ModelRoute.Model` per-step model-id'siyle (AYNI provider) kanıtlanır; gerçek `model_routes`/`model_connections` route/connection seçimi ve LiteLLM'in controller-side model-adapter entegrasyonu E06'nın işidir — **fork, E09'a katlanmaz** (bu session'ın mimari kararı: engine bizim `palai_engine`'imiz, LiteLLM controller-side adapter).
4. **AgentProfile/AgentRevision, triggers, schedules (ev: E11, `phase-11-automation.md`).** Durable `task`/`todo` registry (T7) session/run-scoped runtime primitive'lerdir, AgentProfile DEĞİL; kalıcı publish-edilen profil + trigger + schedule E11. SUB-001..005 E08-owned; delegation'ın MODEL-driven varyantı (DEL-001) E09'da, config-driven E08'de.
5. **MCP/skills/hooks/remote tools (ev: E12, `phase-12-extensions.md`).** E09 yalnız BUILT-IN tool'ları (task/agent/todo/file/shell/git/push/pr) lands; external tool extension SDK'sı + MCP + hooks E12.
6. **Encrypted SecretRef backend — GitHub App root private key mühürlenmesi (ev: E13, `phase-13-governance-data.md`; LP §7.1).** T3 credential broker short-lived token MINT eder ve minting seam'i E13-ready'dir; ama root GitHub App private key LP-0 file-secret köprüsüyle durur — envelope-encrypted at-rest storage E13 devri. Bu kapanana kadar hiçbir sürüm "production credential handling" iddiası taşımaz.
7. **REG/DEL/APV UAT case'lerinin spec §64 kataloğuna reconciliation'ı (ev: spec update).** Master plan line 410 formal task tasarımını + case'leri bu child plana verdi; ID'ler burada authored (REG-001/002, DEL-001/002, APV-001) ama spec §64.x katalog satırları + prefix konvansiyonu bir sonraki spec revizyonunda resmileştirilir (küçük doc devri, kod devri değil).
8. **Recursive delegation depth>1 OFF kalır (ev: ileri E-series carve-out).** E08 `maxChildDepth=1`/`maxChildFanout=4` sabitleri (`child_dispatch.go:14-15` "E-series carve-out" notu) E09'da DEĞİŞMEZ; model-driven delegation depth'i artırmaz. Per-project delegation config (bound'ları DB'den okuma) o carve-out'un işidir.
