# Palai Agents, Schedules, Triggers ve Webhooks Implementation Plan (E11)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Library-idiom-heavy adımlar (pgx conditional-insert/SKIP LOCKED occurrence claim, Go `time.Location`/tzdata DST semantiği, `net/http` custom DialContext ile per-attempt DNS re-resolve, `crypto/hmac` constant-time verify) brief'lerinde Context7 grounding alır (repo politikası, ledger 2026-07-17).

**Goal:** E08 durable Session + E09 workspace/coding + E10 checkpoint/recovery substrate'i üstüne `MASTER-SPEC.md` §10/§21.4-21.7/§32/§33/§34 otomasyon katmanını koymak: AgentProfile/immutable AgentRevision publication + RunTemplateRevision; Trigger revisions + bounded input mapping + source dedupe + session correlation + concurrency policies; PostgreSQL timer-backed five-field cron (timezone/DST/deterministic occurrence/bounded misfire); outbound webhook raw-body HMAC + retries + DNS/redirect safety + redelivery + dead-letter; inbound signed webhook'un durable ack'i + asynchronous run start. Exit gate (master plan §8/E11): **duplicated inbound event ve scheduler replica tek canonical action üretir; callback failure run sonucunu silmez.** Journey 63.4 (scheduled investigation) otomasyon zinciri + E10 kill/recovery adımıyla canlı kanıtlanır.

**Architecture:** Yeni çekirdek icat edilmez; E11 **yalnız control-plane** işidir — yeni engine frame'i YOK, engine/supervisor/runner koduna dokunulmaz (tek contract değişikliği: yeni event tipleri `event-types.json` + `make generate` + drift gate). Tüketilen hazır seam'ler:
1. **ExecutionSpec/ConfigSnapshot çözümlemesi** (§14, `apps/control-plane/internal/execution/config.go` + migration 000005): spec §14 "Responses, ConfigRevisions, RunTemplates, AgentRevisions, Triggers ve ChildRuns TEK logical ExecutionSpec'ten çözülür" der. AgentRevision/RunTemplateRevision, E08'in immutable ConfigSnapshot disiplinine yeni bir KAYNAK ekler — yeni snapshot kavramı eklenmez; run'ın snapshot provenance'ı revision id taşır.
2. **Tek admission yolu:** trigger/schedule'dan doğan run, API'nin kullandığı AYNI execute yolundan girer (`api.Admitter` → execution). Spec §61 "separate loops for API/chat/cron" REJECTED — E11 paralel bir çalıştırma döngüsü kurmaz; TriggerDelivery `admitted→run_created` geçişi mevcut run-creation'ı çağırır. Yeni command kind da YOK (`commands.go:46` seti değişmez) — trigger run BAŞLATIR, command değildir.
3. **Supervised-loop kalıbı** (`packages/coordinator/supervise.go` + retention Reaper + E10 T3 GC + `reconciler.go`): scheduler tick, webhook delivery pump ve inbound-delivery reconcile aynı kalıpta süpervizyonlu iç loop'lardır; E11 farkı, bunun ilk KULLANICI-GÖRÜNÜR scheduler'ı olmasıdır (phase-10 §7.6'nın devri tam burada kapanır).
4. **Event journal** (E04): outbound webhook delivery, `events` journal'ının bir projeksiyonudur — ikinci bir event sistemi kurulmaz; endpoint filter'ları journal'dan okur.
5. **Idempotency substrate** (E04, `api/middleware/idempotency.go`): AUT-013 external-orchestrator retry, mevcut `Idempotency-Key` + trigger dedupe-key mekanizmalarının üstünde kanıtlanır; yeni idempotency makinesi yazılmaz.
6. **E10 recovery makinesi:** schedule/trigger/delivery UZUN ÖMÜRLÜ durable resource'lardır ama RUN değildirler — kesintiden dönüşleri checkpoint ladder'ı DEĞİL, kendi durable-row + misfire/reconcile semantiğidir (§33.3/§34.2). Trigger'ın BAŞLATTIĞI run ise normal run'dır ve E10 ladder'ıyla kurtarılır (journey 63.4 adım 5-6). Bu ayrım her task'ta dürüstçe adlandırılır.

Cron için yeni dependency alınmaz (ponytail): spec §33.2 zaten "explicitly documented five-field syntax" + KENDİ DST politikamızı (duplicate → earlier-instant-once; nonexistent → misfire policy) ister; robfig/cron'un semantiği bunlara birebir uymaz. Küçük in-house parser + `time.Location` tabanlı next-occurrence, property testleriyle yazılır.

**E10 devri bu planda sahiplidir:** phase-10 §7.6 "kullanıcı-görünür scheduler E11" — T3 bunu kapatır. E09 kaydı "AgentProfile/triggers/schedules → E11" (phase-09 §2 Dahil-değil) — T1/T2/T3 kapatır. Durable `task`/`todo` registry (E09 T7) AgentProfile DEĞİLDİR ve bu planda ona dokunulmaz.

**Master plan bağı:** §7 M7 (otomasyon yarısı; MCP/skills yarısı E12); §8 E11 (satır 438-453); §63.4 scheduled investigation journey. UAT sahipliği: AGT-001..003; AUT-001..013 (SH-Beta'da AUT subset — queue-adapter maturity notu §9 matriste; gerçek broker adapter'ları bu dilimde DEĞİL, aşağıda §2). AGT-003 profile-free run E08'de kanıtlıdır (E08 UAT sahipliği); E11 payı, agent yüzeyi VARKEN profile-free yolun regresyonsuz kalmasıdır. Bütün case ID'leri spec §64 kataloğunda ZATEN mevcuttur — E09/E10'un aksine authored-case devri yoktur.

---

## 1. Execution gate

- [ ] `main` >= **E10 merge tip `0d5c23f`** ("Merge E10 T9: recovery UAT catalog + evidence release + kill+recovery journey" — T1-T9 + recovery-0.1.0 main'de, ağaca karşı doğrulandı 2026-07-21). E11, E10'un checkpoint/ladder/tool-ledger yüzeylerini journey'de ve T6 irreversible-guard'ında tüketir; foundation olmadan başlanmaz.
- [ ] Evidence intact: `make evidence-verify` `local-live-0.1.0` + `interactive-0.1.0` + `coding-0.1.0` + `recovery-0.1.0` PASS; `make verify` yeşil baseline.
- [ ] Her task RED-first TDD (master plan §14). Proof sınıfları §10.2; **daha düşük sınıfla pass edilemez** (line 641): `component-real` webhook testleri gerçek local HTTP + gerçek HMAC ile koşar, sahte imza doğrulaması GEÇMEZ; `fault-live` scheduler outage gerçek loop/process durdurmadır, sleep-simülasyonu GEÇMEZ; `live-provider` fake ile GEÇMEZ.
- [ ] Her task gerçek-provider **LIVE smoke** ve/veya gerçek **fault/component smoke** ile biter (kullanıcı politikası). İki pinli gerçek (2026-07-21): (i) E09 T4 forced-tool kalıbı = broker-seam `CASE=coding-tools` (`apps/control-plane/internal/execution/tools/live`, `tool_choice: required`); (ii) **orchestrator `dispatchModel` tool şemasını hâlâ ADVERTISE ETMEZ** — E10'un dört live recovery case'i `PALAI_LIVE_TOOL_ADVERTISING`'e gate'lidir (`scripts/uat/recovery`, `apps/control-plane/internal/execution/live/container_kill_test.go:50`). E11 canlı smoke'ları buna göre yazılır: trigger→run→callback zinciri single-step gerçek run'la TAM kanıtlanır; run İÇİNDE model→tool iddiası ya broker-seam forced kalıbıyla alınır ya da advertising gate'ini dürüstçe adlandırır.
- [ ] Credential hygiene E09/E10'dan aynen: `.env.local`, `set -a`, hygiene grep (`sk-` + Git PAT + App key + installation token + **webhook signing secret**) = 0; webhook secret'ları evidence/log/delivery-attempt-view'da ASLA düz görünmez (sanitized excerpt kuralı §21.6).
- [ ] **Migration numaralandırma — PİNLENDİ (E10-final, 2026-07-21):** zincir ucu main'de `000018_tool_call_ledger` (`storage/embed.go:149`). E10 gerçekte şunları indirdi: T1=000015, T2=000016, T6=000017, T7=000018; **T8 migration İNDİRMEDİ** (child-addressing 000016 `delivered_messages`'ı yeniden kullandı). E11 numaraları **merge sırasına** göre atanır (açılış dalgası T1/T4 önce iner, zincirde boşluk kalmaz): **T1=000019 (agents), T4=000020 (webhooks), T2=000021 (triggers), T3=000022 (schedules)**. FK yönü de uyar: schedules→triggers referansı 000021'den sonra gelir; webhooks (000020) hiçbir E11 tablosuna referans vermez.
- [ ] Branch: `feature/phase-11-automation-agents` (base: `0d5c23f`).

## 2. Bu dilimin sınırı

### Dahil

- **AgentProfile/AgentRevision/RunTemplateRevision** (§10, §32.2): profile = named lineage; revision = immutable executable config (§10 alan listesinin bu dilimde ENFORCE edilen alt kümesi: engine revision pin, instructions, model route, tool set + capability restrictions, workspace/repository defaults, approval rules, budgets, delegation policy, output schema, retention policy, labels). Draft edit = YENİ revision; publish explicit; schedule/production trigger revision PİNLER, rollout explicit pin değişimidir. RunTemplateRevision = agent kimliği taklit etmeyen reusable config; basit webhook AgentProfile istemez.
- **Trigger + TriggerDelivery** (§32.1, §32.3-32.6): versiyonlu kaynak-olay→canonical-action eşlemesi; tip seti `manual/api | webhook | cron` bu dilimde CANLI, `queue | integration_event` tipi şemada tanımlı ama ilk gerçek connector'ları sonraki epic'lerde (aşağıda). Bounded declarative input mapping (field select/rename, constant, conditional/default, schema validation, allowed-name SecretRef lookup; network/fs/process YOK; redacted fixture'a karşı preview/test). Delivery SM `received→authenticated→deduplicated→mapped→admitted→run_created` + `rejected/duplicate/failed/deferred`. Dedupe-key expression; correlation (per-event / bounded-key reuse / named-session append / reject-if-active; key hashed + scoped). Concurrency: allow/queue/replace/drop_if_running/coalesce/singleton; replace/coalesce irreversible side effect SONRASI yasak (E10 tool-ledger'dan okunur).
- **Scheduling** (§33): five-field cron + one-time RFC3339; zorunlu IANA timezone; DST duplicate=earlier-once, nonexistent=misfire policy; deterministic occurrence ID (schedule revision + planned UTC instant); misfire skip/fire_once_now/catch_up(bounded)/fail; transactional occurrence insertion; multi-replica tarama + tek canonical action; deterministic bounded jitter (planned_at vs admitted_at görünür); pause admission'ı keser ama aktif run'ı sessizce öldürmez.
- **Outbound webhooks** (§21.4-21.6): endpoint (URL, event filter, pinned API revision, 1-2 aktif signing secret, timeout/retry policy, secret-ref fixed headers, private/loopback default DENY); `Webhook-Id/-Timestamp/-Signature/-Attempt` + raw-body HMAC-SHA-256; 10s 2xx, redirect takip edilmez, her attempt'te egress-policy'li DNS re-resolve; jittered backoff, 72h/20 attempt, dead-letter view + idempotent redelivery; attempt'ler sanitized görünür. Delivery run terminalini ASLA bloklamaz.
- **Inbound webhooks + adapter contract** (§21.7, §34.2-34.4): source-auth persistence'TAN ÖNCE doğrulanır; InboundEvent normalize; source+source_tenant+source_event_id atomic dedupe; **durable ack** (raw event + dedupe + mapping/admission commit'inden sonra) → uzun iş ack'ten SONRA asenkron; ordered-key poison-message default dead-letter+advance; bounded buffer + queue-depth/oldest-age raporlu backpressure.
- **Trigger callback / output mapping** (§32.1, §21.6 son paragraf): run terminal'inden AYRI kendi terminal state'li callback adımı; outbound delivery makinesini yeniden kullanır; AUT-013 canonical idempotency.
- **Journey 63.4** otomasyon zinciri + E10 kill/recovery adımı + E09 fork-coding adımı; evidence release **`automation-0.1.0`**.

### Dahil değil

- **Gerçek queue-broker adapter'ları (SQS/PubSub/Kafka/NATS/RabbitMQ)** → sonraki epic (E16/E17 maturity; §9 matris notu). E11, §34.2 adapter CONTRACT'ını (ack-after-durable, dedupe, backpressure, poison policy) inbound-webhook adapter'ı + gerçek HTTP redelivery ile component-real kanıtlar; AUT-009/010'un SH-Beta subset'i budur ve DÜRÜSTÇE böyle adlandırılır — hiçbir evidence "Kafka destekli" demez.
- **MCP/skills/hooks/knowledge alanları AgentRevision'da** → E12/E17. Revision şemasına ölü config koyulmaz: bu alanlar E12/E17 kendi epic'lerinde şemaya EKLENİR; E11 revision'ı yalnız bu dilimde enforce edilen alanları kabul eder (unknown field reject — honest naming).
- **Slack/GitHub/queue `integration_event` connector'ları** → E17 (SLK ailesi). Tip enumerate edilir, ilk gerçek connector E17.
- **63.4 adım 4'ün "web research citations" tool'u** → E12/E17 (KNO ailesi). Journey'de araştırma adımı mevcut built-in tool'larla (E09 file/shell + pure tools) kapability-ceiling ALTINDA koşar; kanıtlanan şey "capability asla repository/store-write'a genişlemez" invariantıdır, web-search yeteneği değil — case adı bunu söyler.
- **Multi-node scheduler / fleet placement** → E14/E15. AUT-007 replica kanıtı tek PG üstünde iki gerçek scheduler loop'uyla (iki process) alınır; çok-makine drain E14.
- **Envelope-encryption-at-rest (raw inbound payload, webhook signing secret)** → E13 (LP §7.1 ailesi). E11 kısa-retention raw payload + secret-scan + sanitized-view kanıtlar; "encrypted" iddiası E13'ten önce YOK. Signing secret'lar şimdilik SecretRef-hazır kolonda ama LP file-secret köprüsüyle (E13 mühürler — E09 credential-broker'la aynı devir kalıbı).
- **Egress policy resource'u** → E13. E11 default private/loopback DENY + self-host explicit allowlist FLAG'ini verir (§21.4); policy engine E13.
- **SDK webhook helper + otomasyon SDK ergonomileri** (§23.10) → E16. UAT harness API'yi doğrudan sürer.
- **Agent Card / A2A projection** → E17.

## 3. E11 acceptance contract — canlı eşleme

Exit gate: **duplicated inbound event ve scheduler replica TEK canonical action üretir; callback failure run sonucunu SİLMEZ; journey 63.4 pass** (report evidence valid, duplicate input/callback yok, host change görünür, research capability publication'a genişlemez). Case'ler `tests/uat/cases/<ID>/case.yaml` disiplini; isim = gerçekten assert edilen davranış. Bütün ID'ler §64 kataloğunda mevcut — authored case yok.

| Case | Kanıt | Proof class | Ev |
|---|---|---|---|
| AGT-001 | publish sonrası mutation YENİ revision üretir; eski run'ın snapshot provenance'ı değişmez, reproducible | e2e-deterministic + live-provider | T1 |
| AGT-002 | delivery kabulünde exact trigger revision kaydedilir; sonraki profile/trigger edit kabul edilmiş delivery'yi DEĞİŞTİRMEZ | e2e-deterministic | T2 |
| AGT-003 | profile-free run tüm core özelliklerle çalışır (E08'de kanıtlı; E11 payı: agent yüzeyi varken regresyon yok) | e2e-deterministic (regresyon) | T1 |
| AUT-001 | duplicate inbound webhook → TEK TriggerDelivery action'ı; duplicate orijinale link döner | e2e-deterministic (dedupe çekirdeği) + component-real (gerçek HTTP inbound) | T2+T5 |
| AUT-002 | invalid signature / stale timestamp replay → action ÖNCESİ reject + safe audit | component-real (gerçek HMAC) | T5 |
| AUT-003 | mapping/schema failure → failed delivery, billable run YOK | e2e-deterministic | T2 |
| AUT-004 | correlation queue → aynı key'de ordered run/message | e2e-deterministic | T2 |
| AUT-005 | singleton/coalesce/replace → exact dokümante davranış; irreversible effect kaybı YOK | e2e-deterministic | T2 (irreversible guard T6) |
| AUT-006 | cron DST duplicate/nonexistent → policy'ye göre deterministic occurrence | unit + property (gerçek tzdata) | T3 |
| AUT-007 | scheduler replicas → unique occurrence ID, TAM BİR canonical action | component-real (gerçek PG, iki gerçek loop) | T3 |
| AUT-008 | scheduler outage/misfire → configured skip/fire-once/bounded catch-up | fault-live (gerçek loop/process stop) | T3 |
| AUT-009 | queue redelivery → source ack/dedupe duplicate action'ı ENGELLER | component-real (gerçek HTTP redelivery) | T5 |
| AUT-010 | queue flood → backpressure/fairness, bounded memory, depth görünür | component-real | T5 |
| AUT-011 | outbound retry → imzalı attempt'ler, doğru retry/dead-letter, run terminal ETKİLENMEZ | component-real + e2e-deterministic | T4+T6 |
| AUT-012 | DNS rebinding/redirect → per-attempt revalidation private endpoint'i DENY eder | component-real (gerçek resolver kontrolü) | T4 |
| AUT-013 | external orchestrator retry → canonical idempotency, retry çoğaltmaz | e2e-deterministic | T6 |

## 4. Runtime topology delta

```text
E10 (bugün):  run'lar YALNIZ API/session'dan doğar; süpervizyonlu iç loop'lar var (Reaper/GC/reconcile)
              ama kullanıcı-görünür scheduler/trigger/webhook YOK; events journal yalnız SSE'ye akar

E11 (hedef):  AgentProfile ─> AgentRevision (immutable, publish) ─┐
              RunTemplateRevision (profile-free) ─────────────────┤ pinned by
                                                                  ▼
              Trigger(rev) ── input mapping ── dedupe ── correlation ── concurrency
                 ▲    ▲    └─> TriggerDelivery SM: received→authenticated→deduplicated
                 │    │                            →mapped→admitted→run_created
                 │    │                            (rejected/duplicate/failed/deferred)
                 │    └── inbound signed webhook (verify → normalize → durable ack → async)
                 └────── Schedule(cron+tz) ─> occurrence (deterministic ID, misfire, jitter)
                                              [supervised tick loop; N replica → 1 action]
              run_created ─> AYNI admission/execute yolu ─> Run/Attempt (E10 recoverable)
                                └─ terminal ─> callback (kendi terminal state'i; run'ı bloklamaz)
              events journal ─> webhook pump ─> signed outbound delivery
                                (HMAC raw-body, DNS re-resolve, backoff, dead-letter, redeliver)
```

- **Yeni engine frame YOK; yeni command kind YOK.** Contract deltası yalnız: yeni resource şemaları (agent/trigger/schedule/webhook-endpoint) + yeni event tipleri `agent.revision.published.v1`, `trigger.delivery.*`, `schedule.occurrence.*`, `webhook.delivery.*` — `event-types.json` + `make generate` + drift gate (LP Task 9 kalıbı).
- **API yüzeyi** spec §20.2.1 automation ailesi: `/v1/agents(+revisions,publish)`, `/v1/triggers(+deliveries)`, `/v1/schedules(+occurrences)`, `/v1/webhook-endpoints`, `/v1/webhook-deliveries(+redeliver)` — mevcut `api.NewRouter` + middleware (auth/idempotency/problem) kalıbıyla.
- **Scheduler PG-timer'dır:** tick loop `next_fire_at <= now()` taraması + conditional occurrence INSERT (unique occurrence ID) — LISTEN/NOTIFY veya external timer yok; coordinator supervised-loop kalıbı.
- Dependency direction korunur: state-machines pure (TriggerDelivery SM `packages/state-machines/`); automation domain logic `apps/control-plane/internal/automation/`; HTTP dışa-arama yalnız `adapters/integrations/webhook/`; engine DB/S3/secret GÖRMEZ.
- Migration'lar pinli (merge sırası = DAG açılış sırası): **T1=000019_agents, T4=000020_webhooks, T2=000021_triggers, T3=000022_schedules**. Her biri küçük + idempotent-re-run + down testli; `storage/embed.go` kayıt kalıbı 000018 rider yorumunu (`embed.go:144-152`) örnek alır.

## 5. Task plan

DAG (SPEED REGIME): **T1 ve T4 bağımsız açılış dalgası** (T1 config-substrate'e, T4 event-journal'a dayanır; birbirine değmez); T2←T1 (pinned revision hedefi); T3←T2 (occurrence→delivery admission); T5←T2 (inbound→delivery pipeline); T6←T2+T4 (callback = trigger alanı + outbound makinesi); T7←hepsi. LIVE/fault smoke'lar serialized (paylaşılan `:local` tag).

### Task 1: AgentProfile / immutable AgentRevision / RunTemplateRevision + ExecutionSpec resolution (AGT-001, AGT-003)

**Files:** Create `storage/migrations/000019_agents.up.sql`/`.down.sql` (agent_profiles, agent_revisions, run_template_revisions; revision rows immutable — UPDATE yok, publish flag conditional), `apps/control-plane/internal/automation/agents.go` (create/revise/publish + pin çözümlemesi), `apps/control-plane/api/agents.go` (§20.2.1 agents ailesi), `storage/queries/agents.sql`; Modify `apps/control-plane/internal/execution/config.go` (ExecutionSpec kaynağı olarak AgentRevision/RunTemplateRevision — ConfigSnapshot provenance'a `agent_revision_id` alanı; §14.3 profile+overrides önceliği), `storage/embed.go`, contract schema (agent resource + `agent.revision.published.v1`) + `make generate`.

Semantik (§10, §14, §32.2): revision IMMUTABLE executable config — bu dilimde enforce edilen alan alt kümesi §2'de; unknown/E12-alanı REJECT (ölü config yok). Draft edit yeni revision; publish explicit ve geri alınamaz (rollback = eski revision'a re-pin). Run revision'dan doğduğunda ConfigSnapshot mekanizması AYNEN kullanılır — snapshot revision id'yi provenance olarak taşır; revision sonradan edit'lense bile eski run'ın snapshot'ı değişmez (AGT-001'in "old run remains reproducible" yarısı E08 snapshot invariantından bedava gelir, test yine de pinler). Capability ceiling: revision'ın tool set/capability kısıtı run'ın efektif tool kaydına kesişim olarak iner — revision'da olmayan tool model'e HİÇ sunulmaz (63.4 "research capability never expands" zemini). RunTemplateRevision aynı bileşenler eksi kimlik/delegation — profile-free otomasyon (AGT-003'ün E11 yüzü).

- [ ] Failing testler: `TestAgentRevisionPublishIsImmutable` (publish sonrası PATCH → YENİ draft revision; published row byte-değişmez — DB-level immutability assert); `TestRunFromRevisionSnapshotCarriesRevisionProvenance` (AGT-001: run'ın ConfigSnapshot'ı revision id taşır; revision'ın sonraki revizyonu eski snapshot'ı DEĞİŞTİRMEZ); `TestRevisionToolCeilingIntersectsRunTools` (revision tool set'i dışındaki kayıtlı tool model'e sunulmaz — efektif tool listesi assert); `TestUnpublishedRevisionCannotBePinnedOrRun`; `TestRunTemplateRevisionStartsProfileFreeRun` (AGT-003 E11 yüzü: template'ten run, AgentProfile YOK); `TestUnknownRevisionFieldsRejected` (E12 alanları şimdi reject); `TestProfileFreeResponsesPathUnchanged` (AGT-003 regresyon: mevcut `/v1/responses` yolu agent tablosu varken bit-değişmez davranır); migration idempotent + down.
- [ ] Fail'i doğrula; minimum implementasyon; API testleri mevcut router/middleware kalıbıyla (auth + problem + idempotency).
- [ ] **LIVE smoke:** gerçek `provider-one` run'ı PUBLISHED AgentRevision'dan başlar (instructions + model pin revision'dan, gerçek chatcmpl id). Tool-ceiling yarısı E09 broker-seam forced-tool kalıbıyla (`CASE=coding-tools` emsali, `tool_choice: required`) alınır — **`dispatchModel` tool advertise etmediği için** "revision-dışı tool sunulmadı" assert'i efektif tool registry / request-construction üstünden yapılır, model-spontane redde dayanmaz. Honest ceiling: modelin "agent kişiliği" değil, config-pin + ceiling'in gerçek provider yolunda çalıştığı kanıtlanır; orchestrator-advertising gate'i (PALAI_LIVE_TOOL_ADVERTISING) bu task'ta düşürülmez.

**Verify:** `make verify && make test-component && make test-e2e && make test-live-provider PROVIDER=provider-one CASE=agent-revision-run`.

### Task 2: Trigger + TriggerDelivery SM + input mapping + dedupe + correlation + concurrency (AGT-002, AUT-001/003/004/005)

**Files:** Create `storage/migrations/000021_triggers.up.sql`/`.down.sql` (triggers, trigger_revisions, trigger_deliveries; dedupe unique index; correlation key hash kolonu), `packages/state-machines/trigger_delivery.go` (§32.4 SM — registry-sync + property, E02 kalıbı), `apps/control-plane/internal/automation/mapping.go` (bounded declarative mapping + redacted-fixture preview), `apps/control-plane/internal/automation/deliver.go` (authenticate→dedupe→map→admit pipeline; correlation; concurrency policies), `apps/control-plane/api/triggers.go` (`/v1/triggers` + `POST .../deliveries` manual/api tipi + `GET /v1/trigger-deliveries/{id}`), `storage/queries/triggers.sql`; Modify contract schema (trigger resource + `trigger.delivery.*` event'leri) + `make generate`, `apps/control-plane/cmd/palai-control-plane/main.go` (deferred-delivery reconcile supervised loop — Reaper kalıbı).

Semantik (§32): trigger edit = YENİ revision; kabul edilen delivery, exact revision'ı async işlem ÖNCESİ kaydeder (AGT-002). Her source delivery kendi kimliğini taşır ve SIFIR-veya-BİR canonical action'a link olur; duplicate ORİJİNAL linkage'ı döner (AUT-001 çekirdeği — inbound HTTP yüzü T5). Mapping dili bounded — network/fs/process YOK; SecretRef yalnız allowed-name lookup; mapping hatası = failed delivery, billable run YOK (AUT-003). Correlation §32.5 dört mod; key length-bounded + hashed + (project, trigger revision, source tenant) scoped; reuse authorization/retention'ı BYPASS ETMEZ. **Named-session append yeni substrate İCAT ETMEZ (pin, 2026-07-21):** E09 T7'nin `send_to_agent`/`child_messages` substrate'i ağaçta YOK (grep=0); E10 T8'in adresleme kalıbı aynen kullanılır — session → `ActiveRootRun` (latest non-terminal run; `storage/queries/commands.sql:76`, `packages/coordinator/commands.go:107`) + boundary-durable `delivered_messages` satırı (migration 000016, `InsertDeliveredMessage`); yeni command kind YOK. Concurrency §32.6 altı policy; `queue` = correlation key başına FIFO (AUT-004); replace yalnız "non-side-effecting" işi iptal eder; **irreversible-sonrası replace/coalesce yasağının enforce'u T6'dadır** (E10 tool-ledger okuması — burada policy alanı + doc). `run_created` geçişi run'ı AYNI admission'dan başlatır — paralel loop yok (§61).

- [ ] Failing testler: `TestTriggerDeliveryTransitions` (SM pure; registry-sync + property); `TestDuplicateDeliveryLinksOriginalSingleAction` (AUT-001: aynı dedupe key iki kez → tek run, ikinci delivery `duplicate` + orijinal link); `TestMappingFailureFailedDeliveryNoRun` (AUT-003: schema-invalid input → `failed`, run tablosunda satır YOK); `TestMappingLanguageDeniesNetworkFsSecretEscape` (bounded dil negative corpus'u: url fetch / path / allowed-list dışı secret adı → reject); `TestMappingPreviewAgainstRedactedFixture` (§32.3 preview/test yolu); `TestAcceptedDeliveryPinsExactRevision` (AGT-002: delivery kabul → revision N kaydı; trigger edit → revision N+1; delivery N'le işlenir); `TestCorrelationModesScopedAndBounded` (§32.5 dört mod + key hash + scope; reject-if-active yolu dahil); `TestQueuePolicyOrdersRunsPerKey` (AUT-004: aynı key üç event → FIFO sıra, farklı key paralel); `TestConcurrencyPoliciesDocumentedOutcomes` (AUT-005 deterministik yarı: drop_if_running `skipped` KAYDEDER; singleton trigger-genelinde tek aktif; coalesce deterministic reducer; replace side-effect'siz genç işi iptal eder); `TestDeliveryRunEntersSameAdmissionPath` (run provenance'ı normal execute yolundan — ayrı loop imzası yok); migration idempotent + down.
- [ ] Fail'i doğrula; minimum pipeline; deferred → supervised reconcile turu (yeni scheduler İCAT edilmez — Reaper kalıbı).
- [ ] **LIVE smoke:** manual/API trigger (`POST /v1/triggers/{id}/deliveries`) pinned AgentRevision'la GERÇEK provider run'ı başlatır; aynı dedupe-key'li ikinci POST orijinal delivery linkini döner ve İKİNCİ provider çağrısı OLMAZ (provider request sayısı assert). Honest ceiling: kaynak manual/API'dir; imzalı inbound HTTP T5'te.

**Verify:** `make verify && make test-component && make test-e2e TEST=automation && make test-live-provider PROVIDER=provider-one CASE=trigger-dedupe-run`.

### Task 3: Scheduler — five-field cron, timezone/DST, deterministic occurrence, misfire, replicas, jitter (AUT-006/007/008)

**Files:** Create `storage/migrations/000022_schedules.up.sql`/`.down.sql` (schedules, schedule_occurrences; occurrence_id unique), `apps/control-plane/internal/automation/cron.go` (in-house five-field parser + `time.Location` next-occurrence; §33.2 DST kuralları), `apps/control-plane/internal/automation/scheduler.go` (supervised tick loop: due-scan → transactional occurrence INSERT → trigger delivery admission; misfire; deterministic jitter), `apps/control-plane/api/schedules.go` (`/v1/schedules` + occurrences listesi), `tests/fault/scheduler/` (outage/misfire/replica harness), `storage/queries/schedules.sql`; Modify `apps/control-plane/cmd/palai-control-plane/main.go` (scheduler loop süpervizyona), contract schema (`schedule.occurrence.*`) + `make generate`.

Semantik (§33): cron beş alan, dokümante syntax — parser'ımız yalnız onu kabul eder (robfig/cron alınmadı; DST politikamız §33.2 spesifik: duplicate local time earlier-instant'ta BİR kez; nonexistent misfire policy'ye düşer). Occurrence ID = f(schedule revision, planned UTC instant) — deterministik; saat oynaması duplicate ID üretmez. §33.4: revision + occurrence insertion transactional; N replica tarayabilir, unique occurrence ID TEK canonical action garanti eder (conditional INSERT — E09 fencing guarded-INSERT idiyomu); occurrence run'DAN ÖNCE durable; retry aynı occurrence + idempotency key'i yeniden kullanır; pause gelecek admission'ı keser, aktif run'ı sessizce iptal ETMEZ; schedule silme historical occurrence'ları retention altında KORUR. Misfire §33.3: default fire_once_now + bounded grace; catch_up ASLA sınırsız. Jitter §33.5: per-occurrence deterministik, bounded, planned_at vs admitted_at olarak GÖRÜNÜR; start/end sınırı dışına taşıyamaz. **Scheduler'ın kendisi run değildir — outage kurtarması checkpoint ladder DEĞİL, durable occurrence + misfire policy'dir; bu ayrım evidence'ta adlandırılır.**

- [ ] Failing testler: `TestCronParserAcceptsOnlyDocumentedFiveField` (kabul + red corpus'u; seconds/`@daily` reddi); `TestNextOccurrenceDSTDuplicateFiresOnceEarlierInstant` (AUT-006; gerçek tzdata `Europe/Istanbul`/`America/New_York` fall-back saati); `TestNextOccurrenceDSTNonexistentFollowsMisfirePolicy` (AUT-006; spring-forward saati); `TestOccurrenceIDDeterministicAcrossClockChange` (aynı revision+instant → aynı ID; NTP-jump simülasyonu duplicate üretmez); `TestTwoSchedulerReplicasSingleCanonicalOccurrence` (AUT-007; gerçek PG'de iki eşzamanlı loop yarışır → tek INSERT kazanır, tek delivery); `TestMisfirePoliciesBounded` (AUT-008 deterministik yarı: skip/fire_once_now/catch_up(max_catch_up)/fail dört policy dokümante sonuç; catch_up sınırı aşılamaz); `TestOccurrenceDurableBeforeRunCreated`; `TestPauseStopsAdmissionKeepsActiveRun`; `TestJitterDeterministicBoundedVisible`; `TestDeletedSchedulePreservesOccurrences`; migration idempotent + down.
- [ ] Fail'i doğrula; minimum parser + loop; parser property-test'i (rastgele geçerli expr → next-occurrence monotonic + alan sınırları içinde).
- [ ] **Fault + LIVE smoke:** (fault) gerçek scheduler loop'u planned fire'ı KAPSAYAN pencerede durdurulur (process stop — sleep-simülasyonu değil), restart → misfire policy'ye göre bounded catch-up, occurrence ID'lerde duplicate YOK (AUT-008 fault-live). (LIVE) her-dakika cron → occurrence → trigger delivery → GERÇEK provider run'ı TAM BİR kez; occurrence→delivery→run zinciri evidence'ta. Honest ceiling: tek makine, iki replica aynı PG'de — çok-host scheduler E14.

**Verify:** `make verify && make test-component && make test-fault CASE=scheduler && make test-live-provider PROVIDER=provider-one CASE=cron-fires-once`.

### Task 4: Outbound webhooks — HMAC signing, egress safety, retry/dead-letter, redelivery (AUT-011 delivery-yarısı, AUT-012)

**Files:** Create `storage/migrations/000020_webhooks.up.sql`/`.down.sql` (webhook_endpoints, webhook_deliveries, delivery_attempts), `adapters/integrations/webhook/signer.go` (raw-body HMAC-SHA-256; `Webhook-Id/-Timestamp/-Signature/-Attempt`; 1-2 aktif secret rotation), `adapters/integrations/webhook/sender.go` (10s timeout; redirect follow YOK; her attempt'te egress-policy'li DNS re-resolve — custom `DialContext` resolved-IP pinning; private/loopback default DENY + self-host explicit allowlist flag), `apps/control-plane/internal/automation/webhook_pump.go` (events journal → filter → delivery; jittered exponential backoff; 72h/20 attempt; dead-letter; supervised), `apps/control-plane/api/webhook_endpoints.go` (`/v1/webhook-endpoints`, `/v1/webhook-deliveries`, `POST .../redeliver`), `storage/queries/webhooks.sql`; Modify contract schema (`webhook.delivery.*`) + `make generate`, `main.go` (pump süpervizyona).

Semantik (§21.4-21.6): imza girdisi version + delivery id + timestamp + EXACT raw body; constant-time compare; timestamp toleransı configurable (default 5dk); rotation overlap'inde iki imza da geçerli. Delivery: 2xx tamamlar; redirect izlenmez (3xx terminal-deny); DNS her attempt'te egress policy'den re-resolve — resolve-then-connect aynı IP'ye pinlenir (rebinding'in TOCTOU'su kapanır, AUT-012); retry yalnız network hatası + 408/425/429/5xx (+dokümante 409); 72h/20 attempt sonrası dead-letter view + alert-görünürlük; her attempt sanitized status/duration/body-excerpt ile görünür (secret sızmaz); operator redelivery = AYNI delivery ID + AYNI payload. Delivery run terminalini asla bloklamaz (AUT-011'in delivery yarısı; callback bağı T6).

- [ ] Failing testler: `TestSignatureCoversRawBodyAndVerifiesConstantTime` (bilinen-vektör HMAC + byte-oynanmış body reddi); `TestRotationBothSecretsVerifyWindowBounded`; `TestPrivateAndLoopbackDestinationsDeniedByDefault` (AUT-012 statik yarı: RFC1918/loopback/link-local URL → create veya attempt reject; self-host allowlist flag'i ile izin); `TestDNSRebindingReResolveDeniesFlippedTarget` (AUT-012 dinamik yarı: test resolver ilk çözümde public, ikincide private döner → ikinci attempt DENY; bağlantı resolved-IP'ye pinli); `TestRedirectNotFollowed` (301/302/307/308 → terminal, Location'a istek YOK — gerçek local HTTP server); `TestRetryMatrixAndDeadLetterBounds` (2xx/404-terminal/429-retry/500-retry sınıflandırması; 20 attempt sonrası dead-letter; backoff jitter bounded — clock injection'la, sleep'siz); `TestRedeliveryReusesDeliveryIdAndPayload`; `TestAttemptViewSanitizedNoSecret` (imza secret'ı + secret-ref header değeri excerpt'te YOK); `TestPumpSupervisedEndpointDownDoesNotStarveOthers` (bir endpoint down → diğer endpoint'lerin delivery'si akar); migration idempotent + down.
- [ ] Fail'i doğrula; minimum signer/sender/pump; hygiene grep'e webhook signing secret pattern'i eklenir.
- [ ] **Component smoke (gerçek HTTP):** compose stack'te gerçek local receiver'a imzalı delivery; receiver GERÇEK HMAC doğrular; 5xx-then-2xx senaryosunda retry timeline + tek başarılı teslim; dead-letter'a düşen delivery redeliver API'siyle AYNI id/payload'la canlanır. Honest ceiling: model davranışı yok — bu task saf infra; canlı model bağı T6/T7'de.

**Verify:** `make verify && make test-component TEST=webhook && make test-e2e TEST=automation`.

### Task 5: Inbound signed webhooks + InboundEvent + durable ack + adapter contract (AUT-001 inbound, AUT-002/009/010)

**Files:** Create `adapters/integrations/webhook/inbound.go` (source-connection imza doğrulaması — persistence ÖNCESİ; §21.7 InboundEvent normalize; timestamp tolerance + Webhook-Id dedupe), `apps/control-plane/internal/automation/inbound.go` (durable kayıt + source+tenant+event_id atomic dedupe → T2 delivery pipeline'ına teslim; ack-after-durable; bounded buffer + backpressure §34.4; ordered-key poison policy §34.3), `apps/control-plane/api/inbound_webhooks.go` (public inbound endpoint — trigger'a bağlı, auth source-imzasıyla), `tests/fault/scheduler/inbound_crash_test.go`; Modify `storage/queries/triggers.sql` (inbound dedupe sorguları — tablo T2'de; yeni migration YOK, inbound raw-payload kolonları T2 migration'ında tanımlanır), contract schema + `make generate`.

Semantik (§21.7, §34.2-34.4): doğrulama persistence'tan ÖNCE — invalid signature/stale timestamp action ÖNCESİ reject + safe audit (AUT-002); raw payload policy izin verirse kısa-retention kaydedilir (encryption-at-rest iddiası E13). **Durable ack sırası:** source auth kabul → raw/normalized event durable → dedupe commit → mapping/admission commit VEYA terminal reject → ANCAK O ZAMAN 2xx; uzun iş ack'ten sonra asenkron. Ack'lenmiş ama `run_created`'a varmamış delivery'yi supervised reconcile turu (T2) tamamlar — **crash-arası kayıp yok, duplicate yok; bu, trigger'ların E10-dünyasındaki "durable + resumable" kanıtıdır (run-checkpoint ladder'ı DEĞİL — delivery kendi durable-row makinesiyle döner). İsim/yapı hizası PİNLİ (E10-final):** E10 kalıbı iki sweep-loop'tur — `apps/control-plane/internal/execution/reconciler.go` (supervisor adı "reconciler": dead-letter + approval sweep) ve `reconcile.go` (adı "tool-reconciler": uncertain-tool; kendi başlığı "reuses the retention/GC supervised-loop shape (reconciler.go), not a new framework" der). T2/T5 delivery-reconcile AYNI şekli alır: `Run(ctx)` sweep loop'u, `main.go`'daki mevcut Supervisor'a (`apps/control-plane/cmd/palai-control-plane/main.go:174-184` emsali) **"delivery-reconciler"** adıyla asılır; yeni framework yok. Redelivery (AUT-009): kaynak ack'i kaybolup AYNI event yeniden gelirse dedupe orijinal linkage döner. Backpressure (AUT-010): bounded buffer; eşik aşımında consumption/kabul yavaşlatılır (429 + Retry-After), admission queue depth + oldest age raporlar; broker flood'u control-plane memory'sini şişiremez. Ordered key poison: mapping'i patlayan event default dead-letter-view'a düşer ve key İLERLER.

- [ ] Failing testler: `TestInvalidSignatureRejectedBeforePersistence` (AUT-002; bozuk imza → DB'de delivery satırı YOK, audit event VAR); `TestStaleTimestampReplayRejected` (tolerans dışı timestamp → reject; Webhook-Id dedupe replay'i); `TestAckOnlyAfterDurableRecordAndDedupe` (2xx anında raw event + dedupe row committed — test hook'la sıra assert); `TestDuplicateSourceEventSingleActionOriginalLinkage` (AUT-001 inbound yüzü: aynı source+tenant+event_id iki gerçek HTTP POST → tek run); `TestAckedDeliveryCrashBeforeRunRecoversExactlyOnce` (fault: ack ↔ run_created arası process kill → restart sonrası reconcile TAM BİR run üretir); `TestRedeliveryAfterLostAckDoesNotDuplicate` (AUT-009: 2xx kaybolmuş gibi aynı event tekrar → dedupe); `TestFloodBoundsMemoryReportsDepthApplies429` (AUT-010: gerçek HTTP burst → buffer sınırı, 429+Retry-After, depth/oldest-age metriği); `TestPoisonEventDeadLettersAndAdvancesOrderedKey` (§34.3 default).
- [ ] Fail'i doğrula; minimum inbound + backpressure; public endpoint'te tenant çözümlemesi trigger üzerinden (source imzası auth'tur — API key değil; yanlış trigger'a imza cross-check reddi).
- [ ] **Fault + LIVE smoke:** GERÇEK imzalı inbound POST (harness HMAC hesaplar) → durable ack → asenkron GERÇEK provider run'ı; aynı event'in duplicate POST'u → tek run (provider çağrı sayısı assert); ack-sonrası-crash varyantı reconcile ile TAM BİR run. Honest ceiling: kaynak bizim harness'imizdir (Slack/GitHub connector E17); kanıt imza+dedupe+durable-ack zinciridir.

**Verify:** `make verify && make test-component TEST=webhook && make test-e2e TEST=automation && make test-fault CASE=scheduler && make test-live-provider PROVIDER=provider-one CASE=inbound-webhook-run`.

### Task 6: Trigger callback + idempotency + irreversible-concurrency guard (AUT-011 bağ-yarısı, AUT-013, AUT-005 guard)

**Files:** Create `apps/control-plane/internal/automation/callback.go` (run terminal → output mapping → outbound delivery T4 makinesiyle; kendi terminal state'i); Modify `apps/control-plane/internal/automation/deliver.go` (replace/coalesce admission'ında irreversible-effect kontrolü — E10 tool_calls ledger sorgusu), `apps/control-plane/api/triggers.go` (delivery POST'unda `Idempotency-Key` — mevcut middleware), `storage/queries/triggers.sql` (callback state kolonları — T2 migration'ında tanımlı, yeni migration YOK), contract schema + `make generate`.

Semantik (§32.1, §21.6 son paragraf, §32.6 son cümle): callback ayrı bir adımdır — run terminal'i callback'ten BAĞIMSIZ kalır; callback failure retry/dead-letter'a düşer ama run sonucu/evidence'ı SİLİNMEZ (AUT-011 bağ-yarısı; 63.4 pass koşulu "canonical result recoverable even if output update fails" ile aynı ilke). Trigger "workflow success = acknowledged delivery" isterse bu, callback'in KENDİ terminal state'idir. AUT-013: external orchestrator'ın retry'ları — `POST deliveries` + `Idempotency-Key` (E04 middleware) VE dedupe-key — canonical tek action; retry hiçbir katmanda çoğalmaz (delivery, run, callback üçü de tek). **Irreversible guard (§32.6):** replace/coalesce, hedef run'ın E10 tool-ledger'ında `irreversible` sınıfı EXECUTE edilmiş bir call varsa trigger-specific reconciliation contract olmadan REDDEDİLİR. **Ledger adları PİNLİ (E10-final):** YENİ TABLO YOK — `000018_tool_call_ledger` mevcut `tool_calls` tablosuna (000001) ALTER rider'ıdır. Guard sorgusu `tool_calls`'un rider kolonlarını okur: `replay_class` (`pure|idempotent|reversible|irreversible|interactive`, default `'pure'`) + `reconciliation_state` (`''|reconciling|reconciled_completed|reconciled_not_applied|manual_resolution`); diğer rider kolonları `request_hash`/`external_idempotency_key`/`lease_owner`/`commit_boundary`. Uncertain alt-sistemi `apps/control-plane/internal/execution/reconcile.go`. Guard = hedef run'da `replay_class='irreversible'` olup EXECUTE edilmiş (completed ya da `uncertain`) satır var mı.

- [ ] Failing testler: `TestCallbackFailureLeavesRunTerminalIntact` (AUT-011: callback hedefi kalıcı 500 → dead-letter; run `completed` + evidence erişilir kalır); `TestCallbackDeliveredOncePerDeliveryAcrossRetries` (5xx-then-2xx → alıcıda TEK semantic callback — Webhook-Id dedupe alıcı tarafında assert); `TestOrchestratorRetrySameIdempotencyKeySingleEverything` (AUT-013: aynı Idempotency-Key üç POST → tek delivery/run/callback); `TestOrchestratorRetryDifferentKeySameDedupeSingleAction` (idempotency-key farklı ama dedupe-key aynı → yine tek action, duplicate linkage); `TestReplaceDeniedAfterIrreversibleToolCall` (E10 ledger'da irreversible execute edilmiş → replace admission reject + görünür sebep); `TestCoalesceDeniedAfterIrreversibleToolCall`; `TestOutputMappingBoundedSameLanguageAsInput` (T2 mapping dili yeniden kullanılır — ikinci dil İCAT EDİLMEZ).
- [ ] Fail'i doğrula; minimum callback + guard; callback outbound T4 pump'ından akar (ayrı sender yazılmaz).
- [ ] **LIVE smoke:** callback'li trigger → GERÇEK provider run tamamlanır → imzalı callback gerçek local receiver'a injected 5xx-then-2xx altında TAM BİR kez teslim; run evidence callback öyküsünden bağımsız intact. Honest ceiling: receiver bizim harness'imiz; kanıt run-terminal/callback ayrışması + teklik.

**Verify:** `make verify && make test-component && make test-e2e TEST=automation && make test-live-provider PROVIDER=provider-one CASE=callback-once`.

### Task 7: Journey 63.4 + UAT materialization + automation-0.1.0 evidence → E11 kapanışı

**Files:** Create `tests/e2e/automation/` journey harness'i, `tests/uat/cases/AGT-001..002/` + `AUT-001..013/` `case.yaml` (AGT-003 mevcut E08 case'ine E11 regresyon notu), yeni evidence release `automation-0.1.0`; Modify `Makefile` (`uat-automation` hedefi + `test-fault CASE=scheduler`; önceki `uat-*` hedefleri DOKUNULMAZ), `tests/uat/evidence.go` (yalnız gerekiyorsa otomasyon-alanı kuralları: occurrence-teklik + callback-teklik kanıtı — RecoveryProof kuralı E10'dan hazır).

- [ ] Failing test (deterministic tier): `TestScheduledInvestigationJourneyDeterministic` — 63.4 TAM zincir: (1) AgentRevision + strict output schema publish; (2) signed webhook + cron trigger varyantları; (3) duplicated event → tek delivery/run (AUT-001); (4) run, capability ceiling altında koşar — repository/store-write tool'ları revision'da YOK, deneme reddedilir; (5-6) **E10 kill harness'iyle** mid-run kill → ladder recovery → TEK rapor (RecoveryProof; **harness yüzeyi PİNLİ (E10-final):** gerçek kill = `make test-fault CASE=recovery` → `tests/fault/recovery/` — `docker rm -f`, label `io.palai.sandbox=engine`, digest `PALAI_RUNNER_ENGINE_IMAGE_ID`; deterministik journey çekirdeği = `TestCodingJourneyWithKillRecoveryDeterministic`, `E2E_RUN=... TEST=responses scripts/test/e2e` üzerinden; `make uat-recovery` → `scripts/uat/recovery`; RecoveryProof kuralı `tests/uat/evidence.go` — `RecoveryProof.Complete()` gate'i, eksik/incomplete proof PASS SAYILMAZ); (7) TAM BİR idempotent callback; (8) attach + follow-up (E08 yolu); (9) ayrı yetkili coding-session fork (E09 harness'i). Pass: rapor şemaya valid, duplicate input/callback YOK, host change evidence'ta görünür, research capability publication'a GENİŞLEMEZ.
- [ ] Canlı journey (`live-provider` + `fault-live` + component-real webhook; compose stack, gerçek OpenAI; E09/E10 hygiene disiplini aynen): cron VE inbound-webhook varyantları gerçek HMAC'la; mid-run container kill (E10 T5 harness'i) → recovery → tek rapor; callback gerçek receiver'a tek teslim; evidence manifest: occurrence ID tekliği, delivery dedupe linkage'ı, RecoveryProof, callback receipt tekliği, credential+signing-secret absence scan.
- [ ] Evidence verifier: `automation-0.1.0` `VerifyRelease` PASS; secret finding 0; duplicated-event case'i orijinal-linkage kanıtı olmadan PASS SAYILMAZ.
- [ ] `make uat-automation PROVIDER=provider-one` PASS; deterministik tier aynı journey'i fake provider + hook'lu kill'le CI'da koşar, canlı tier onsuz GEÇMEZ.

**Verify:** `make verify && make test-e2e && make test-fault && make uat-automation PROVIDER=provider-one && make evidence-verify RELEASE=automation-0.1.0`.

## 6. Final release check (E11 exit gate)

- [ ] `make verify`, `make test-component`, `make test-e2e`, `make test-fault` (scheduler dahil), `make test-security` PASS.
- [ ] `make uat-automation PROVIDER=provider-one` bütün bu-dilim case'leri PASS; hiçbir `component-real` webhook case'i sahte HMAC/receiver'la, hiçbir `fault-live` case simüle outage'la geçmedi.
- [ ] `make evidence-verify RELEASE=automation-0.1.0` PASS; secret + signing-secret finding 0.
- [ ] **Exit gate cümlesi kanıtlı (master plan §8/E11):** duplicated inbound event → tek canonical action (orijinal linkage'lı); iki scheduler replica → tek canonical occurrence/action; callback failure run sonucunu SİLMEDİ. Journey 63.4 pass koşulları (§63.4) evidence'ta.
- [ ] Regresyon yok: `make uat-local-live`, `make uat-interactive`, `make uat-coding`, `make uat-recovery` (PROVIDER=provider-one) hâlâ PASS — automation tabloları/loop'ları mevcut yolları bozmadı.
- [ ] `git status --short` temiz; generated drift 0 (`make generate` sonrası diff yok — yeni resource + event şemaları pinli).
- [ ] M7'nin otomasyon yarısı kapanır; "automation beta" iddiası E12 (MCP/skills) kapanmadan verilmez (M7 satırı iki epic'liktir).

## 7. Bu plana girmeyen devirler (kayıt yeri: bu commit'li plan)

1. **Gerçek queue-broker adapter'ları — SQS/PubSub/Kafka/NATS/RabbitMQ (ev: E16/E17 maturity; §9 matris notu "queue adapters maturity'ye bağlı").** E11 §34.2 CONTRACT'ı inbound-webhook adapter'ı üstünde kanıtladı (AUT-009/010 SH-Beta subset'i); broker-spesifik visibility/lease-extension ve gerçek broker smoke'ları adapter epic'ine.
2. **`integration_event` connector'ları — Slack/GitHub vb. (ev: E17, SLK ailesi; journey 63.3).** Tip enumerate edildi; ilk gerçek connector E17.
3. **MCP/skills/hooks/knowledge alanlarının AgentRevision şemasına eklenmesi (ev: E12/E17).** E11 revision'ı bilinmeyen alanı REDDEDER; E12 şemayı genişletirken bu reject testi güncellenir (bilinçli çakışma — sessiz ölü config'e tercih edildi).
4. **Web-research/citation tool'u (ev: E12/E17, KNO ailesi).** Journey 63.4 adım 4 mevcut built-in tool'lar + capability ceiling ile koşturuldu; "web research" yeteneği iddia edilmedi.
5. **Envelope-encryption-at-rest — raw inbound payload + webhook signing secret + SecretRef backend (ev: E13; LP §7.1 ailesi).** E11 kısa-retention + sanitized-view + secret-scan kanıtlar; "encrypted" iddiası E13'ten önce YOK. Egress policy resource'u da E13 (E11 default-deny + explicit allowlist flag verdi).
6. **Multi-node scheduler + fleet drain (ev: E14/E15).** AUT-007 tek PG + iki loop'la kanıtlı; çok-host scheduler placement E14.
7. **SDK webhook helper + otomasyon ergonomileri (ev: E16; §23.10).** UAT harness API'yi doğrudan sürdü.
8. **Agent Card / A2A projection (ev: E17; A2A ailesi).**
9. **Master plan satır 440 child-plan adı `phase-11-automation.md` yazar; gerçek dosya `phase-11-automation-agents.md` (bu dosya) — pointer düzeltmesi sonraki master-plan touch'ında (doc devri).**
10. **AMEND-LATER kaydı — KAPANDI (E10 merge `0d5c23f`'e karşı, 2026-07-21):** Plan E10 T3-sonrası main'e (`33a99e9`) karşı yazılmıştı; üç nokta ağaç doğrulamasıyla pinlendi: **(a) migration numaraları** — zincir ucu `000018_tool_call_ledger`; E10 T8 migration indirmedi; E11 = T1=000019 / T4=000020 / T2=000021 / T3=000022 (merge sırası, §1); **(b) T6 guard** — tablo `tool_call_ledger` DEĞİL, `tool_calls` üstünde 000018 rider'ı; kolonlar `replay_class`/`reconciliation_state` (+`request_hash`/`external_idempotency_key`/`lease_owner`/`commit_boundary`); uncertain alt-sistemi `execution/reconcile.go`; **(c) T7 harness/`uat-recovery` yüzeyi + T5 "delivery-reconciler" isim hizası** task metinlerinde pinli. E10 T8'in detached-child semantiği (release/wake/rebind — `execution/finalize.go:132`, `detach.go`; adresleme = `ActiveRootRun` + `delivered_messages`, YENİ command kind yok) T7 journey adım 5-6 metnini DEĞİŞTİRMEDİ; adım 8 attach yolu E08'dir. Ayrıca T2 correlation'ın named-session append'i bu adresleme kalıbını yeniden kullanır (E09 T7 `send_to_agent`/`child_messages` substrate'i ağaçta YOK — grep=0). Kalan tek taşınan gerçek: E10 live smoke'larının tool-advertising ceiling'i (`PALAI_LIVE_TOOL_ADVERTISING`) E11'de de geçerli — §1 canlı-smoke maddesi buna göre yazıldı.
