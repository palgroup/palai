# Palai MCP, Skills, Hooks ve Remote Tools Implementation Plan (E12)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Library-idiom-heavy adımlar (MCP 2025-11-25 JSON-RPC framing, JSON Schema 2020-12 validation, Go `crypto/hmac`/`crypto/ed25519` signed-request, archive/decompression-bomb safe extraction, OCI stdio attach) brief'lerinde Context7/spec grounding alır (repo politikası, ledger 2026-07-17).

**Goal:** E11'in kapattığı otomasyon yarısının üstüne `MASTER-SPEC.md` §28 extensibility katmanını koymak: tool/tool-set registry (§28.2-28.4), **orchestrator'ın gerçek provider'a tool şeması ADVERTISE etmesi** (E08'den beri taşınan ceiling'in çözümü — §6), remote HTTP tool protokolü + imzalı transport + late-callback reconciliation (§28.24-28.25; TOL-016/017'nin E12 yarısı), MCP stdio/Streamable HTTP client + namespacing + güvenlik savunmaları (§28.13-28.14), skill quarantine + digest pin + no-authority (§28.15-28.16), hook kategorileri + izole yürütme (§28.17), web-research tool'u (journey 63.4 adım 4 devri) ve tool SDK parity (§28.23). Exit gate (master plan §8/E12): **malicious skill/MCP metadata capability genişletemez; extension crash core process'i düşürmez.**

**Architecture:** Yeni çekirdek icat edilmez; her extension çağrısı MEVCUT dispatch yolundan akar:
1. **Tek tool-dispatch yolu:** engine `tool.request` frame'i → `apps/control-plane/internal/execution/orchestrator.go:440` → `dispatchTool` → `packages/tool-broker/broker.go:156` `Execute` (request-hash, fenced lease, ledger, replay-class). MCP/remote/skill-script tool'ları bu pipeline'a YENİ `Tool` kayıtları olarak girer — ikinci bir dispatch döngüsü YOK (§61 "separate loops" REJECTED emsali; E11 admission disipliniyle aynı ilke).
2. **Advertising plumbing'i zaten iki uçta hazır:** `packages/model-broker/types.go:66` `Request.Tools []ToolSchema` kontratta; `adapters/models/provider_one/adapter.go:249-251` tool'ları provider isteğine serialize ediyor, `:139-163` streamed tool_call delta'larını parse ediyor; engine `loop.py` `AWAITING_TOOLS` state'i ile multi-step tool turn'ünü E06'dan beri destekliyor. Eksik olan TEK tel: `apps/control-plane/internal/execution/model_dispatch.go:115-127` `modelbroker.Request`'i `Tools` OLMADAN kuruyor (ağaca karşı doğrulandı 2026-07-22). T1 bu teli bağlar — yeni engine frame'i GEREKMEZ.
3. **Egress güvenliği hazır seam:** E11 T4'ün `adapters/integrations/webhook/sender.go` makinesi (`VetDestinationURL:173`, `VetDestination:202`, `pinnedDial:141` resolved-IP pinning + private/loopback deny) MCP-over-HTTP, remote tool ve web-research egress'inde AYNEN yeniden kullanılır — ikinci SSRF makinesi yazılmaz.
4. **Ledger/reconciliation hazır seam:** TOL-016/017'nin ledger/fence yarısı E10'da kanıtlı (`tool_calls` 000018 rider kolonları; `apps/control-plane/internal/execution/reconcile.go` uncertain alt-sistemi). E12 aynı case ID'lerin **signed-transport + SDK yarısını** alır (phase-10 §7 devir 3 — shared-case disiplini).
5. **Registry/revision kalıbı:** immutable revision + publish + unknown-field reject E11 T1'de kurulu (`apps/control-plane/internal/automation/agents.go:64` strict decode). Tool/skill/hook revision'ları aynı kalıbı alır; AgentRevision şeması bu dilimde extension alanlarıyla GENİŞLER ve E11'in reject testi bilinçli güncellenir (phase-11 §7 devir 3).
6. **Contract deltası:** yeni resource şemaları (tool, tool-set, mcp-connection, skill, hook) + `tool-http.v1` envelope + yeni event tipleri → `protocols/` schemas + `make generate` + drift gate (LP Task 9 kalıbı). **Yeni engine frame kind YOK, yeni command kind YOK** — `tool.request`/`tool.result`/`model.request` seti değişmez; MCP progress'i mevcut tool event'lerine map edilir.

**Master plan bağı:** §7 M7'nin extensions yarısı — **doğrulandı 2026-07-22:** master plan §7 tablosunda M7 = "Automation & extensions" (E11 otomasyon + E12 MCP/skills, satır 230), M8 = "Governance & data safety" (E13, satır 231); bu plandaki bütün milestone iddiaları M7'dir, "M8" etiketi HİÇBİR yerde E12 için kullanılmaz ("automation beta" iddiası E12 kapanınca verilir — phase-11 §6 son madde; master-plan pointer notu §8.7). §8 E12 (satır 454-469). UAT sahipliği: TOL-008..012, TOL-016..018 (+TOL-014/015 web-research genişletmesi regresyon olarak). Authored case'ler (E09 REG/DEL/APV kalıbı): EXT-001..006 (§3 tablosu + T10 materialization; taslaktaki "..008" aralığı düzeltildi — 007/008 tanımlı değil) — §64 kataloğunda olmayan advertising/registry/web-research/crash-isolation kanıtları bu dilimde `tests/uat/cases/` altında materialize edilir (§64 katalog reconciliation devri).

---

## 1. Execution gate + cross-cutting constraints

- [ ] `main` >= **E11 merge tip `61e7c48`** ("Merge E11 T7: automation journey + UAT catalog + automation-0.1.0 evidence + residuals sweep" — E11 T1-T7 COMPLETE, ağaca karşı doğrulandı 2026-07-22). `make uat-automation` hedefi + `automation-0.1.0` evidence release main'de MEVCUT (doğrulandı); yoksa başlanmaz.
- [ ] Evidence intact: `make evidence-verify` önceki beş release PASS (`local-live-0.1.0`, `interactive-0.1.0`, `coding-0.1.0`, `recovery-0.1.0`, `automation-0.1.0`); `make verify` yeşil baseline.
- [ ] Her task RED-first TDD (master plan §14); proof sınıfları §10.2, **daha düşük sınıfla pass edilemez**: `component-real` MCP testleri gerçek MCP server process'i/gerçek local HTTP ile koşar, mock JSON-RPC GEÇMEZ; `live-provider` fake ile GEÇMEZ; security corpus'u gerçek malicious fixture'larla koşar.
- [ ] Her task gerçek-provider **LIVE smoke** ve/veya gerçek **component/security smoke** ile biter. **T1 main'e indiği andan itibaren canlı smoke'lar SPONTAN tool-use ile yazılır** (forced-tool broker-seam kalıbı yalnız T1-öncesi paralel başlayan task'larda geçici olarak kullanılır ve evidence'ta böyle adlandırılır); `PALAI_LIVE_TOOL_ADVERTISING` gate'i T1'de SİLİNİR (§6).
- [ ] Credential hygiene E09/E10/E11'den genişletilir: `.env.local`, `set -a`, hygiene grep mevcut marker'lara (`sk-`, Git PAT, App key, installation token, `whsec_`) **MCP connection token, remote-tool signing secret, search-API key** pattern'lerini ekler; hiçbir extension credential'ı engine frame/event/log/artifact/snapshot'ta düz görünmez (TOL-013 invariantı yeni executor'lara taşınır).
- [ ] Honest naming: her case adı gerçekten assert edilen davranış; "MCP destekli" iddiası yalnız 2025-11-25 revision + kanıtlı transport'lar için; resources/prompts E17'ye ertelendiği evidence'ta adlandırılır.
- [ ] **§61 tek-admission disiplini:** MCP/remote/hook hiçbir yeni çalıştırma döngüsü kurmaz; her tool çağrısı `dispatchTool` → tool-broker pipeline'ından, her model adımı `dispatchModel`'den geçer. Yeni command kind YOK; contract deltası yalnız resource şemaları + `tool-http.v1` + event tipleri (`make generate` + drift 0).
- [ ] **Migration numaralandırma — PİNLENDİ (E11-final, 2026-07-22):** zincir ucu main'de `000023_inbound_trigger_auth` (E11 T5; `storage/migrations/` ağaca karşı doğrulandı — E11 T6/T7 migration İNDİRMEDİ). E12 numaraları merge sırasına göre: **T2=000024_tools (açılış dalgası, tek migration), T4=000025_remote_tools, T5=000026_mcp, T7=000027_skills (ikinci dalga, paralel worktree'ler), T8=000028_hooks (üçüncü dalga)** (§4). Fiili merge sırası 000025/26/27 atamasından saparsa henüz merge edilmemiş migration'lar MERGE ANINDA yeniden numaralandırılır (E11 §1 kalıbı — pin anında değil, çünkü paralel dalganın sırası pin'de bilinemez); zincirde boşluk yasak.
- [ ] Branch: `feature/phase-12-extensibility` (base: E11 merge tip).

## 2. Bu dilimin sınırı

### Dahil

- **Tool advertising → gerçek provider** (T1): `dispatchModel` efektif tool set şemalarını `Request.Tools`'a koyar; E10'un dört gate'li live case'i gate'siz koşar.
- **Tool/ToolSetRevision registry + `/v1/tools`, `/v1/tool-sets`** (§28.2-28.4 alan alt kümesi; §20.2 rotaları): namespaced canonical ad (`publisher.namespace.tool`), immutable revision + digest, model-visible kısa ad + collision check, replay-class/timeout/limit deklarasyonu. Built-in'ler (file/shell/commit/push/PR/task — `apps/control-plane/internal/execution/tools/`) KOD-tanımlı kalır; registry satırları remote/MCP executor'ları içindir.
- **Remote HTTP tool protokolü** (§28.24-28.25): `tool-http.v1` envelope, HMAC signed request (timestamp + body digest + `Idempotency-Key`=tool_call_id), 200/202/409/RFC9457, signed one-use callback token, fence-gated once-only callback, late-callback → E10 reconciliation; redirects off + `VetDestination` egress.
- **MCP client** (§28.13): stdio (OCI-container-backed sandboxed server) + Streamable HTTP; discovery → connection-namespaced ToolRevision; call/progress/cancel → mevcut tool event'leri; trust level + capability ceiling per connection. **MCP güvenlik** (§28.14): audience/resource-bound token, token-passthrough YASAK, Origin validation, SSRF-korumalı metadata fetch, sampling default-DENY + enabled yolu ayrı brokered/budgeted model step (TOL-010).
- **Skills** (§28.15-28.16): Agent Skills folder convention import; SkillRevision (digest pin, scan result, required tools/capabilities); quarantine install pipeline (traversal/symlink/special-file/decompression-bomb reddi + secret/executable scan); progressive loading (SKILL.md metadata → context reference); **no-authority invariant** — skill hiçbir capability VERMEZ.
- **Hooks** (§28.17 alt kümesi): `before_tool`/`after_tool`/`before_model`/`on_terminal`/`before_repository_publish` noktaları; policy (sync, fail-closed) / transform (schema-validated patch, fail-closed) / observer (async, fail-open) kategorileri; timeout-bounded; tenant hook'u API process'inde ASLA koşmaz — T4 signed remote-worker transport'u yeniden kullanılır.
- **Web-research tool** (journey 63.4 adım 4 devri; phase-11 §7 devir 4): control_plane executor built-in fetch + citation'lı normalized result (§28.6 citations alanı); `VetDestination` egress + TLS zorunlu + bounded output → artifact (TOL-015 kalıbı); capability tipli grant, store/repository-write'a GENİŞLEMEZ.
- **Extension SDK + conformance** (§28.23; TOL-018): Go (`packages/extension-sdk/`) + TypeScript (`sdks/typescript/` içine paket) helper'ları — schema emit, signed-invocation verify, normalized result, tool_call_id idempotency; paylaşılan fixture corpus'u `tests/conformance/tool-sdk/`. Python helper minimal (aynı corpus'u geçen tek modül).
- **Journey + `uat-extensibility` + `extensibility-0.1.0`** evidence release; `tests/security/extensions/` negative corpus (malicious skill arşivi, sahte MCP metadata, token-passthrough denemesi, SSRF hedefleri, hook crash).

### Dahil değil

- **MCP resources/prompts/elicitation** → E17 (KNO ailesi; context ingestion policy oraya). E12 yalnız tools+progress+cancel+sampling-policy; evidence bunu adlandırır.
- **Interactive OAuth/PKCE authorization-code flow** → E17 (web console yok; §28.14 "where applicable"). E12: SecretRef static token + audience binding + passthrough yasağı + exact-redirect/PKCE validation'ının PASİF doğrulayıcısı (konfigüre edilmişse şart koşar).
- **Signed WebAssembly hook runtime** → sonraki epic (E16+). E12 hook izolasyonu = platform-authored in-process deterministic policy'ler + tenant kodu için T4 remote-worker yolu; "wasm" iddiası YOK.
- **Envelope-encrypted SecretRef backend** → E13 (LP §7.1). MCP/remote-tool secret'ları E09 credential-broker kalıbıyla file-secret köprüsünde durur; E13 mühürler.
- **Sigstore/SBOM/in-toto provenance zorunluluğu** (§28.20) → E18. E12 digest-pin + publisher alanı + signature-VARSA-doğrula verir; supply-chain policy E18.
- **Extension bundle/plugin manifest + lifecycle UI** (§28.18-28.19 tam yüzeyi) → E16/E17. E12 lifecycle'ın çekirdeğini (quarantine→approved→enabled + side-by-side revision + circuit breaker) tool/skill/MCP üstünde verir; genel "plugin" paketi sonra.
- **Search-provider entegrasyonu** web-research için → **E17 (KNO) — KARAR (pin 2026-07-22, §8.3):** E12 çekirdeği fetch+citations'tır ve HİÇBİR search-API bağımlılığı/secret'ı almaz; gerçek arama backend'i (Brave/SerpAPI vb.) bir Connection + secret + egress-policy işidir ve E17 knowledge katmanına gider. T3 bununla BLOKLANMAZ; "web search" İDDİA EDİLMEZ, "web research (fetch+cite)" denir.
- **capability_worker / client executor'ları** (§28.1) → E15/E16-17. Executor enum'unda tanımlı, ilk gerçek implementasyon değil.
- **İkinci provider adapter'ı** → E16. Advertising parity fake+provider-one üstünde kanıtlanır.

## 3. E12 acceptance contract — canlı eşleme

| Case | Kanıt | Proof class | Ev |
|---|---|---|---|
| EXT-001 | gerçek provider advertised şemayla SPONTAN tool_call üretir; multi-step model→tool→model turn canonical event'lerle terminal olur | live-provider | T1 |
| EXT-002 | efektif set dışındaki kayıtlı tool provider isteğinde ASLA görünmez (request-construction assert + revision-ceiling live yarısı) | e2e-deterministic + live-provider | T1 |
| EXT-003 | tool/tool-set revision immutable + namespaced; collision reddi; model-visible kısa ad deterministik | e2e-deterministic | T2 |
| EXT-004 | web-research sonucu citations taşır; capability repository/store-write'a genişlemez; private/metadata hedef DENY (TOL-014 genişletme) | component-real + live-provider | T3 |
| TOL-016 (signed yarısı) | imzasız/yanlış-imzalı remote istek REDDEDİLİR; signed request + stable tool_call_id → TEK execution (ledger yarısı E10'dan) | component-real (gerçek local HTTP + gerçek HMAC) | T4 |
| TOL-017 (signed yarısı) | timeout sonrası GEÇ signed callback → fence/reconciliation stale commit'i reddeder; callback token one-use | component-real + fault | T4 |
| TOL-008 | MCP stdio+HTTP discovery/call/progress/cancel mapping + connection namespacing PASS | component-real (gerçek MCP server process) | T5 |
| TOL-009 | token-passthrough/confused-deputy denemesi audience validation'la DENY; upstream token reuse YOK | security (component-real) | T6 |
| TOL-010 | sampling default DENY; enabled çağrı AYRI brokered budgeted model step olarak görünür | component-real + live-provider | T6 |
| TOL-011 | malicious skill arşivi/instructions quarantine/path-scan/capability boundary'yle execution/escalation'a ULAŞAMAZ | security (gerçek malicious fixture) | T7 |
| TOL-012 | hook timeout/crash kategori fail-mode'una uyar; core process ayakta kalır | component-real + fault | T8 |
| TOL-018 | TS/Go(/Py minimal) fixture'ları eşdeğer şema/result/signature üretir | conformance (shared corpus) | T9 |
| EXT-005 | MCP server crash / remote tool down / hook crash → circuit breaker + tool_unavailable; run görünür fail, control-plane düşmez | fault-live | T10 |
| EXT-006 | MCP tool description/annotation değişikliği yeni revision + re-approval gerektirir; sessiz capability drift YOK | e2e-deterministic (security) | T5+T10 |

## 4. Runtime topology delta + migration planı

```text
E11 (bugün): model.request → dispatchModel ──[Tools: YOK]──> provider   (canlı run'lar single-step)
             tool.request  → dispatchTool → tool-broker (built-in'ler; forced-seam live kanıtı)

E12 (hedef): model.request → dispatchModel ──[Tools: efektif set şemaları]──> provider
                                  ▲ spontan tool_calls geri döner (adapter.go:139 zaten parse ediyor)
             tool.request → dispatchTool → tool-broker Execute
                ├─ sandbox/control_plane (built-in + web-research)      [kod-tanımlı]
                ├─ remote_http ── tool-http.v1 signed ──> müşteri servisi ──202──> signed callback
                │                                          (VetDestination, fence, late→reconcile)
                └─ mcp ── stdio(OCI container) / StreamableHTTP ──> MCP server (namespaced, ceiling'li)
             SkillRevision ─ quarantine→digest-pin→enabled ─> context reference (authority YOK)
             Hooks: before_tool/after_tool/before_model/on_terminal/before_repository_publish
                    (policy=closed, transform=patch, observer=async; tenant kodu remote-worker'da)
```

Migration'lar (merge sırası = DAG açılış sırası; her biri küçük + idempotent-re-run + down testli; `storage/embed.go` kayıt kalıbı):
- **000024_tools** (T2): `tools`, `tool_revisions`, `tool_set_revisions` (immutable revision satırları, digest, namespace unique index).
- **000025_remote_tools** (T4): `remote_tool_operations` (202 operation id, callback token hash, deadline) — `tool_calls`'a FK; signing-secret ref kolonu connection'da.
- **000026_mcp** (T5): `mcp_connections` (+trust level, capability ceiling, transport, secret ref), discovered-revision bağ tablosu.
- **000027_skills** (T7): `skills`, `skill_revisions` (digest, scan result, quarantine state).
- **000028_hooks** (T8): `hooks` (point, category, timeout, fail mode, target).

## 5. Task plan

DAG (SPEED REGIME): **T1, T2, T3 bağımsız açılış dalgası** (T1 model_dispatch teline, T2 registry substrate'ine, T3 webhook-egress seam'ine dayanır; birbirine değmez — T3'ün spontan-live yarısı T1 main'e inince güçlenir). T4←T2, T5←T2, T7←T2 (ikinci dalga, paralel worktree'ler); T6←T5, T8←T4, T9←T4 (üçüncü dalga); T10←hepsi. LIVE smoke'lar serialized (paylaşılan compose stack + `:local` tag).

### Task 1: Tool advertising → gerçek provider — ceiling çözümü (EXT-001, EXT-002)

**Files:** Modify `apps/control-plane/internal/execution/model_dispatch.go` (Request'e `Tools:` — efektif set şemaları), `packages/tool-broker/broker.go` (`Tool`'a `Description` alanı + `Schemas() []modelbroker.ToolSchema` benzeri listing; `Tool.InputSchema` zaten var `broker.go:58`), `apps/control-plane/internal/execution/tools/*.go` (built-in'lere model-visible description), `apps/control-plane/cmd/palai-control-plane/main.go:140` (broker kurulumu değişmez — yalnız description'lar); Delete/Modify — **gate temizliği (`git grep PALAI_LIVE_TOOL_ADVERTISING` = 0 şartı BÜTÜN referansları kapsar; envanter ağaca karşı doğrulandı 2026-07-22):** env-guard'lı üç test `apps/control-plane/internal/execution/live/container_kill_test.go:50`, `host_kill_test.go:42` (+`:50` `t.Fatal` — live harness'i bu task'ta WIRE edilir, aşağıdaki kabul kriteri), `restore_test.go:82`; tasarım-yorumu gate'li iki test `detached_test.go` (çok-adımlı parent↔child conversation yarısı aktive edilir) ve `tools/live/agent_revision_live_test.go` (forced-call → advertised/spontan yükseltme); yorum referansları `apps/control-plane/internal/automation/live/{callback_once,cron_fires_once,inbound_webhook,trigger_dedupe}_live_test.go`; script'ler `scripts/test/live-provider` (yorumlar) + `scripts/uat/recovery` (live tier echo-yönergesi gerçek per-case koşuya döner).

Semantik: efektif set = run'ın kayıtlı tool'ları ∩ AgentRevision ceiling'i (E11 T1 kesişimi) — `dispatchModel` yalnız bu setin şemalarını gönderir; `dispatchTool` zaten yalnız kayıtlı tool'u çalıştırır (model uydurma adla bypass edemez, §28.5). Fake adapter parity: fake provider da `Tools`'u görür ve deterministik tool_call fabrikasyonu ARTIK şema-doğrulamalı olur (mevcut deterministic testler bit-değişmez kalmalı — regresyon gate'i). Tool description'ları untrusted-claim disiplinindedir (§28.2) — prompt-injection yüzeyi büyüdüğü için description'lar sabit platform metnidir, tenant metni T2 registry'de approval'a bağlanır.

- [ ] Failing testler: `TestDispatchModelAdvertisesEffectiveToolSchemas` (Request.Tools = efektif set; şema alanları InputSchema'dan); `TestToolOutsideEffectiveSetNeverAdvertised` (EXT-002: revision-ceiling dışı kayıtlı tool Request.Tools'ta YOK); `TestAdvertisingPreservesDeterministicFakeRuns` (mevcut e2e fake yolları bit-değişmez); `TestForceToolCallStillWorksWithAdvertising` (E09 forced-seam kalıbı bozulmaz); provider-one adapter'ına yeni test YOK (serialize/parse zaten `adapter.go:249/:139` kanıtlı).
- [ ] Fail'i doğrula; minimum wiring; E10 live testlerindeki gate + skip metinleri kaldırılır (`git grep PALAI_LIVE_TOOL_ADVERTISING` = 0).
- [ ] **Gate-borcu kapanışı (AÇIK T1 kabul kriteri):** E10'un DÖRT gate'li live case'i (`checkpoint-restore`, `container-kill-recovery`, `host-kill-restore`, `detached-child-conversation`) + E09/E11 revision-ceiling live yarısı (`agent-revision-run` — "revision dışı tool ASLA sunulmaz" artık request-construction yerine gerçek ADVERTISED istekte assert edilir; EXT-002 canlı yarısı) skip-guard'sız PASS koşulur (`:local` stack'te serialized). DÜRÜST KAPSAM: `host_kill_test.go:50` bugün `t.Fatal("live harness not yet wired")` taşır — bu case için yalnız gate silmek yetmez, container-engine live harness'inin pause boundary'yi runner yolundan sürmesi de T1 kapsamındadır (dosyadaki "ceiling 3" notu); `detached` case'inin conversation yarısı da bu task'ta ilk kez canlı koşar.
- [ ] **Disposition (2026-07-22, T1 uygulandı):** advertising ceiling KALKTI, `git grep PALAI_LIVE_TOOL_ADVERTISING` = 0 (kod/test/script/tests-uat; yalnız bu plan + phase-11:28 tarihsel istisna). Canlı çalıştırma yer gerçeği: `spontaneous-tool-roundtrip` (YENİ) + `agent-revision-run` (forced→spontan) + `detached-child-conversation` = **gerçek PASS**. Advertising, spontaneous smoke ile **ceiling-4**'te kanıtlandı: gerçek model advertised tool'u SPONTAN çağırır → broker fenced-ledger'dan çalıştırır → dosya diske yazılır (terminal-completion İDDİA EDİLMEZ). `checkpoint-restore` + `container-kill-recovery` **T1b engine-wire tool_call-id follow-up'ına** SKIP eder: attempt-2 continuation assistant tool_call + tool result'ı yeniden thread'ler; engine-wire tipi (`contracts.ToolCall`/engine.schema.json) yalnız name+arguments taşır, provider tool_call id'si `toEngineToolCalls`'da düşer → threaded konuşma gerçek chat API için bozuk (advertising değil, schema+adapter follow-up — T1 schema/adapter değiştirmez). `host-kill-restore` container-engine live harness follow-up'ında SKIP. `detached`'in conversation live yarısı da T1b'ye bağlı (child multi-step = threaded continuation). Latent harness fix: live `subprocessDialer` `supervisor.hello` handshake'ini göndermiyordu (eklendi).
- [ ] **LIVE smoke:** gerçek `provider-one` run'ında model FileTool/ShellTool şemalarını görür ve SPONTAN çağırır (tool_choice zorlaması YOK); model→tool→model çok-adımlı turn canonical event'lerle terminal (gerçek chatcmpl id + tool_call id evidence'ta). Honest ceiling: tek provider; maliyet sınırı için smoke küçük tool setiyle koşar.

**Verify:** `make verify && make test-e2e && make test-live-provider PROVIDER=provider-one CASE=spontaneous-tool-roundtrip && make uat-recovery PROVIDER=provider-one`.

### Task 2: Tool/ToolSetRevision registry + `/v1/tools` + AgentRevision genişletmesi (EXT-003) — migration 000024

**Files:** Create `storage/migrations/000024_tools.up.sql`/`.down.sql`, `apps/control-plane/internal/extensions/registry.go` (create/revise/publish — `automation/agents.go` immutable-revision kalıbı), `apps/control-plane/api/tools.go` (§20.2 `/v1/tools(+revisions)`, `/v1/tool-sets(+revisions)`; mevcut router/middleware kalıbı), `storage/queries/tools.sql`; Modify `apps/control-plane/internal/automation/agents.go:64` (DecodeRevisionInput: `tool_sets`/`mcp_connections`/`skills`/`hooks` alanları artık KABUL — E11'in unknown-reject testi bilinçli güncellenir, phase-11 §7 devir 3), `packages/tool-broker/registry.go` (DB-backed kayıtların broker'a yüklenmesi), contract schema (tool/tool-set resource + event tipleri) + `make generate`, `storage/embed.go`.

Semantik (§28.2-28.4): canonical ad `publisher.namespace.tool` unique + case-sensitive ASCII + length limit; revision immutable (UPDATE yok) + digest; model-visible kısa ad deterministik üretilir + collision-check; ToolSetRevision exact revision pinler + capability kesişimi + yalnız-SIKILAŞAN approval override. Tenant-yazılı description untrusted → publish approval'ı admin action'dır. Built-in'ler registry'ye SEED edilmez (kod-tanımlı; ponytail) — registry yalnız remote/MCP executor referansı taşır; executor config secret'ları SecretRef.

- [ ] Failing testler: `TestToolRevisionImmutableAndDigestPinned`; `TestCanonicalNamespaceCollisionRejected`; `TestModelVisibleShortNameDeterministicCollisionChecked`; `TestToolSetPinsExactRevisionsApprovalOnlyStricter`; `TestAgentRevisionAcceptsExtensionFieldsNow` (E11 reject testinin bilinçli tersi + hâlâ-bilinmeyen alan reject); `TestRegistryToolsLoadIntoBrokerEffectiveSet` (kayıt → broker → efektif set kesişimi); migration idempotent + down.
- [ ] Fail'i doğrula; minimum registry + API; state-machine gerekmez (lifecycle T5/T7 connection/skill üstünde).
- [ ] **LIVE smoke:** registry'ye kaydedilmiş minimal `control_plane` tool (echo-benzeri pure) T1 advertising'iyle gerçek provider'a sunulur ve spontan çağrılır; revision-dışı ikinci tool sunulmaz (EXT-002 registry yüzü). T1 henüz inmediyse forced-seam + honest not (geçici, evidence adlandırır).

**Verify:** `make verify && make test-component && make test-e2e && make test-live-provider PROVIDER=provider-one CASE=registry-tool-roundtrip`.

### Task 3: Web-research tool — fetch + citations + egress (EXT-004; TOL-014/015 genişletme) — migration YOK

**Files:** Create `apps/control-plane/internal/execution/tools/research.go`(+`_test`) (control_plane executor built-in: URL fetch → bounded extract → citations'lı normalized result; büyük içerik → artifact, E09 artifact write-path), Modify `apps/control-plane/cmd/palai-control-plane/main.go:140` (broker kaydı), `apps/control-plane/internal/execution/tools/egress.go` (varsa genişlet — `adapters/integrations/webhook/sender.go` `VetDestination:202` + `pinnedDial:141` yeniden kullanımı; TLS zorunlu, redirect policy'li, size/time bounded).

Semantik (§28.9, §28.6, journey 63.4 adım 4): network capability tipli grant'la açılır; default DENY profillerinde kapalı; private/loopback/link-local/metadata hedefleri resolve-sonrası ve redirect-sonrası DENY (TOL-014'ün tool-yüzü — E11 T4 makinesi aynen); sonuç `citations` (URL + başlık + erişim zamanı) taşır; içerik model text'ine bounded excerpt, tamamı artifact (TOL-015 kalıbı); capability repository/store-write'a GENİŞLEMEZ (63.4 pass koşulu zemini — revision ceiling'i publish tool'larını dışarıda tutar). Search-provider YOK (§8.3) — "web research (fetch+cite)" olarak adlandırılır.

- [ ] Failing testler: `TestResearchFetchProducesCitations`; `TestResearchDeniesPrivateAndMetadataTargetsAfterResolveAndRedirect` (gerçek test resolver + redirect zinciri — sender_test kalıbı); `TestResearchLargeBodyBoundedExcerptPlusArtifact`; `TestResearchRequiresNetworkCapabilityAndNeverPublishCapability` (efektif set kesişim assert'i); `TestResearchTLSRequired`.
- [ ] Fail'i doğrula; minimum fetch+extract (HTML→text stdlib/basit; readability kütüphanesi ALINMAZ — ponytail).
- [ ] **LIVE smoke:** gerçek provider run'ı advertised research tool'unu SPONTAN çağırır (T1 sonrası), gerçek bir public URL'i fetch eder, citations'lı cevap üretir; revision'da publish tool'ları YOK → capability genişlemesi denemesi reddedilir. Honest ceiling: arama yok (verilen URL üstünden research); egress gerçek internete çıkar — CI'da component-real local server, canlıda gerçek URL.

**Verify:** `make verify && make test-component && make test-live-provider PROVIDER=provider-one CASE=web-research-citations`.

### Task 4: Remote HTTP tool — `tool-http.v1` + signed transport + async callback + late reconciliation (TOL-016/017 signed yarısı) — migration 000025

**Files:** Create `storage/migrations/000025_remote_tools.up.sql`/`.down.sql` (remote_tool_operations: operation id, callback token hash, deadline, result hash), `adapters/tools/http/executor.go`(+`_test`) (envelope §28.24; HMAC signed request: timestamp + body digest + `Idempotency-Key`=tool_call_id; `VetDestination`+`pinnedDial` egress; redirects off; 200/202/409/RFC9457), `adapters/tools/http/callback.go` + `apps/control-plane/api/tool_callbacks.go` (broker-controlled callback endpoint; one-use audience-bound token; fence + result-hash gate), `protocols/schemas/tool-http.v1.json` (contract) + `make generate`, `storage/queries/remote_tools.sql`; Modify `packages/tool-broker/broker.go` (remote executor'ın `Tool.Exec` olarak bağlanması — broker pipeline'ı DEĞİŞMEZ), `apps/control-plane/internal/execution/reconcile.go` (late-callback girişi mevcut uncertain akışına).

Semantik (§28.24-28.25): imza girdisi envelope raw body + timestamp (E11 T4 `signer.go` kalıbı; constant-time verify); 202 → durable operation satırı + deadline; callback AKTİF tool lease/fence altında + AYNI result hash'le TAM BİR kez kabul (TOL-016'nın signed yarısı — ledger tekliği E10'dan hazır: `tool_calls` 000018 rider); timeout/cancel sonrası GEÇ callback sessizce uygulanmaz → `reconcile.go` uncertain yoluna girer (TOL-017 signed yarısı; fence yarısı E10'da kanıtlı); progress advisory + terminal-success rapor EDEMEZ; cancellation stable operation id'yle. Executor müşteri servisidir = YENİ TRUST BOUNDARY (§5): kimlik doğrulanmış olsa da dönen içerik untrusted, şema-validate edilir, capability taşımaz.

- [ ] Failing testler: `TestUnsignedOrTamperedRemoteRequestRejected` (TOL-016 signed: imzasız/byte-oynanmış → reject + audit); `TestRemoteDuplicateRetrySameToolCallIdSingleExecution` (gerçek local HTTP sayaç — E10 T7 testinin signed-transport üstünde tekrarı); `TestAsync202CallbackAcceptedOnceUnderFence` (ikinci callback + farklı result hash → reject); `TestLateCallbackAfterDeadlineEntersReconciliationNotSilentCommit` (TOL-017 signed); `TestCallbackTokenOneUseAudienceBound`; `TestRemoteEgressVetted` (private hedefli remote tool endpoint reject); `TestRemoteResultSchemaValidatedUntrusted` (şema-dışı sonuç → failed, capability alanları yok sayılır); migration idempotent + down.
- [ ] Fail'i doğrula; minimum executor+callback; imza şeması `tool-http.v1` kontratına pinlenir (SDK T9 aynı şemayı implement eder).
- [ ] **Component + LIVE smoke:** compose stack'te gerçek local remote-tool server'ı (harness, gerçek HMAC verify eden): sync 200 yolu + async 202→callback yolu + late-callback reddi timeline'la; LIVE: gerçek provider spontan remote tool'u çağırır → 202 → callback → model sonuca devam eder (tam async round-trip gerçek modelle). Honest ceiling: remote server bizim harness'imiz (gerçek müşteri servisi değil); kanıt imza+teklik+fence zinciri.

**Verify:** `make verify && make test-component TEST=remote-tools && make test-live-provider PROVIDER=provider-one CASE=remote-tool-async-roundtrip`.

### Task 5: MCP client — stdio + Streamable HTTP + namespacing + progress/cancel (TOL-008, EXT-006) — migration 000026

**Files:** Create `storage/migrations/000026_mcp.up.sql`/`.down.sql` (mcp_connections + discovered revision bağı), `adapters/integrations/mcp/client.go`(+`_test`) (JSON-RPC framing, 2025-11-25 revision; stdio transport: MCP server'ı `adapters/sandboxes/oci` driver'ıyla AYRI container'da başlat + stdin/stdout attach — E09 volume/spec altyapısı; Streamable HTTP transport: `VetDestination` egress'li client), `adapters/integrations/mcp/discovery.go` (tools/list → connection-namespaced ToolRevision satırları T2 registry'sine; annotations untrusted), `apps/control-plane/api/mcp_connections.go` (`/v1/mcp-connections(+discover)`), `storage/queries/mcp.sql`; Modify `packages/tool-broker/` (mcp executor Exec bağlaması — çağrı `tools/call`, progress notification → mevcut tool progress event'i, cancel → `notifications/cancelled`), contract schema + `make generate`.

Semantik (§28.13, §28.4): her connection explicit trust level + capability ceiling taşır; keşfedilen tool'lar `publisher=connection` namespace'iyle kaydedilir — iki server'ın `search`'ü çakışamaz; discovery admin action'dır, model MCP server EKLEYEMEZ; description/annotation değişikliği YENİ revision + re-approval (EXT-006 — sessiz capability drift yok); progress advisory; cancel deadline'lı; server crash → connection circuit breaker + `tool_unavailable` (EXT-005 zemini; §28.21). Experimental MCP Tasks KULLANILMAZ — platform run'ları kanonik kalır.

**Stdio-container kararı (pin 2026-07-22, §8.4 — reuse-with-named-gap):** MCP server container'ı `adapters/sandboxes/oci` driver'ıyla DOĞRUDAN başlatılır (`driver.go:57` `Spec.Labels` + `docker.go:98` — MCP'ye özel label sınıfı, engine label'larından ayrık); **RunnerGateway'e SOKULMAZ** — engine-container yaşam döngüsü `apps/control-plane/internal/execution/runner_gateway.go`'da kalır (E09 T1 invariantı: yalnız RunnerGateway engine container'ı koşturur). `host_quarantine` (000017) engine-host semantiğidir ve MCP için YENİDEN KULLANILMAZ — MCP failure yüzeyi connection-level circuit breaker'dır (§28.21, bu task'ta). **Adlandırılmış boşluk (T5'te kapatılır):** control-plane crash'i sonrası öksüz MCP container'ları için label-scoped orphan sweep (E10 T3 GC kalıbı; engine sweep'i MCP label'ına DOKUNMAZ, MCP sweep'i engine label'ına DOKUNMAZ). Fallback (control-plane-host child process + rlimit) yalnız driver-attach engeli çıkarsa AÇIK kararla ve güvenlik notuyla alınır.

- [ ] Failing testler: `TestMCPStdioDiscoveryCallProgressCancel` (component-real: gerçek minimal MCP server process'i — testdata'da küçük Python/Go stdio server); `TestMCPHTTPTransportVettedEgress` (private URL'li connection reject); `TestDiscoveredToolsNamespacedByConnectionNoCollision` (TOL-008 namespacing); `TestAnnotationChangeRequiresNewRevisionAndReapproval` (EXT-006); `TestMCPCallFlowsThroughStandardDispatch` (tool_calls ledger satırı + request hash — ikinci dispatch yolu YOK); `TestMCPServerCrashTripsBreakerToolUnavailable`; `TestOrphanMCPContainerSweptByLabelEngineContainersUntouched` (§8.4 named-gap kapanışı); migration idempotent + down.
- [ ] Fail'i doğrula; minimum client (tools yüzeyi); resources/prompts kabul EDİLMEZ (unknown yüzey → görünür unsupported, sessiz atlama değil).
- [ ] **Component + LIVE smoke:** gerçek stdio MCP server (container'da) discover → tool T1 advertising'iyle gerçek provider'a sunulur → model SPONTAN MCP tool'unu çağırır → sonuç canonical event'lerle döner. Honest ceiling: server bizim test server'ımız (üçüncü-parti gerçek MCP server smoke'u T10 journey'de bir public/pinli örnekle değerlendirilir, §8.5); HTTP transport'un canlı yarısı local harness'e karşı.

**Verify:** `make verify && make test-component TEST=mcp && make test-live-provider PROVIDER=provider-one CASE=mcp-tool-roundtrip`.

### Task 6: MCP authorization/security — audience, passthrough yasağı, Origin, SSRF, sampling (TOL-009, TOL-010)

**Files:** Create `adapters/integrations/mcp/auth.go`(+`_test`) (audience/resource-bound token doğrulaması; token-passthrough YASAK — upstream'e giden hiçbir istekte bizim bearer'ımız yok; Origin validation HTTP transport'ta; metadata/discovery fetch'i `VetDestination`'la), `adapters/integrations/mcp/sampling.go` (sampling request → default DENY; enabled: `packages/model-broker` `Route` üstünden AYRI budgeted model step + görünür event — `model_dispatch.go` kalıbı, engine'e DOKUNMADAN control-plane-side step), `tests/security/extensions/mcp_test.go` (confused-deputy corpus'u).

Semantik (§28.14): connection credential'ları SecretRef (E13 köprüsü); token audience'ı connection'ın resource'una bağlı — başka upstream'e replay DENY; MCP server aldığı bearer'la platform endpoint'lerine GERİ ÇAĞIRAMAZ (callback yüzeyi yalnız T4'ün one-use token'ı); sampling enabled ise ayrı Reservation budget'ıyla meter'lanır (TOL-010'un "separate brokered budgeted model step" yarısı) ve child/model step olarak event'lenir.

- [ ] Failing testler: `TestUpstreamTokenNeverForwardedToMCPServer` (TOL-009: istek yakalama — platform bearer'ı hiçbir MCP-bound istekte yok); `TestAudienceMismatchDenied` (connection A token'ı server B'ye replay → deny + audit); `TestHTTPOriginValidated`; `TestMetadataFetchSSRFVetted` (discovery URL'i private'a resolve → deny); `TestSamplingDeniedByDefault` (TOL-010 yarı 1); `TestSamplingEnabledRoutesBrokeredBudgetedVisibleStep` (TOL-010 yarı 2: Reservation + event assert); `TestMCPServerCannotCallPlatformWithReceivedCredentials`.
- [ ] Fail'i doğrula; minimum savunma seti; PKCE/exact-redirect pasif doğrulayıcı (konfigüre edilmişse şart, interactive flow E17 — honest naming).
- [ ] **Security + LIVE smoke:** malicious MCP server fixture'ı (token'ı geri oynamaya, sahte annotation'la capability istemeye, sampling flood'a çalışan) her denemede DENY + audit; LIVE: sampling-enabled connection'da server'ın sampling isteği gerçek provider'a AYRI budgeted step olarak gider ve budget aşımında kesilir. Honest ceiling: OAuth interactive flow yok; static-token audience modeli.

**Verify:** `make verify && make test-security && make test-component TEST=mcp && make test-live-provider PROVIDER=provider-one CASE=mcp-sampling-budgeted`.

### Task 7: Skills — SkillRevision + quarantine + progressive loading + no-authority (TOL-011) — migration 000027

**Files:** Create `storage/migrations/000027_skills.up.sql`/`.down.sql`, `apps/control-plane/internal/extensions/skills.go`(+`_test`) (install by URL/repo = admin action; fetch credentials'sız quarantine'e; digest hesap + pin; SKILL.md parse — Agent Skills convention), `apps/control-plane/internal/extensions/quarantine.go` (arşiv güvenliği: traversal/symlink-escape/special-file/decompression-bomb reddi; static secret + executable-content scan — E09 changeset secret-scan kalıbı yeniden kullanılır), `apps/control-plane/api/skills.go` (`/v1/skills(+revisions)`), `storage/queries/skills.sql`, `tests/security/extensions/skill_test.go` (malicious arşiv corpus'u); Modify run config assembly (`apps/control-plane/internal/execution/config.go` — enabled skill'lerin SKILL.md metadata'sı context reference olarak snapshot'a; body on-demand FileTool'la skill dizininden okunur — progressive loading, engine değişikliği YOK), contract schema + `make generate`.

Semantik (§28.15-28.16): skill = untrusted İÇERİK; hiçbir authority vermez — required tools/capabilities yalnız İSTEKtir, efektif set yine revision-ceiling kesişimidir (E11 T1 + T1/T2 mekanizması); model skill INSTALL edemez; update = yeni revision + capability değiştiyse re-approval; run asla "latest" kullanmaz — digest kaydedilir; scan FAIL → quarantine'de kalır, enable EDİLEMEZ.

- [ ] Failing testler: `TestSkillArchiveTraversalSymlinkBombRejected` (TOL-011 corpus: `../` path, symlink escape, device file, zip bomb — gerçek arşivlerle); `TestSkillInstructionsGrantNoCapability` (SKILL.md "use the push tool" der → efektif set'te push yoksa çağrı reddedilir); `TestSkillDigestPinnedRunRecordsExactDigest`; `TestScanFailureBlocksEnable`; `TestSkillMetadataProgressivelyLoadedNotFullBody` (context snapshot'ta yalnız metadata; body dosya okumasıyla); `TestModelCannotInstallSkill` (tool yüzeyinden install yolu YOK); migration idempotent + down.
- [ ] Fail'i doğrula; minimum pipeline; Agent Skills convention'ının import'u — export E16/E17.
- [ ] **Security + LIVE smoke:** malicious skill fixture'ları (prompt-injection'lı SKILL.md dahil) quarantine/boundary'yi GEÇEMEZ; LIVE: zararsız gerçek skill (ör. "commit-message convention" skill'i) enabled → gerçek provider run'ı metadata'yı görür, body'yi okur, talimatı uygular — ama skill'in istediği ekstra capability VERİLMEMİŞTİR ve denemesi reddedilir (no-authority canlı kanıtı). Honest ceiling: injection savunması capability-boundary'dir, model davranış garantisi değil — case adı bunu söyler.

**Verify:** `make verify && make test-security && make test-component TEST=skills && make test-live-provider PROVIDER=provider-one CASE=skill-no-authority`.

### Task 8: Hooks — kategoriler + timeout/fail-mode + izole yürütme (TOL-012) — migration 000028

**Files:** Create `storage/migrations/000028_hooks.up.sql`/`.down.sql`, `apps/control-plane/internal/extensions/hooks.go`(+`_test`) (hook registry + beş nokta: `before_tool`/`after_tool` → `tool_dispatch.go` seam; `before_model` → `model_dispatch.go`; `on_terminal` → `finalize.go`; `before_repository_publish` → E09 publish yolu `publication_registry.go`; kategori dispatch: policy=sync deterministic fail-CLOSED network'süz; transform=schema-validated patch izinli alanlara fail-CLOSED; observer=async fail-OPEN sonucu etkileyemez), `apps/control-plane/api/hooks.go` (`/v1/hooks`), `storage/queries/hooks.sql`, `tests/security/extensions/hook_test.go`; Modify contract schema + `make generate`.

Semantik (§28.17): tenant kodu API process'inde ASLA — tenant hook'u T4 signed remote-worker çağrısıdır (transport reuse; wasm E16+ §2); platform-authored policy hook'ları in-process deterministic (network I/O yapamaz — tip sistemiyle değil kod disipliniyle: exec fonksiyonu yalnız input struct alır); timeout kategori başına bounded; hook output capability VEREMEZ (transform patch şeması capability alanlarını içermez); tanımsız hook noktası REJECT (ölü config yok — beş nokta dışındakiler `§28.17` tam listesinden E13+/consuming-epic'lere).

- [ ] Failing testler: `TestPolicyHookTimeoutFailsClosed` (TOL-012 yarı: before_tool policy hook'u asılır → tool çağrısı reddedilir, run görünür); `TestObserverHookCrashFailsOpenRunUnaffected` (TOL-012 yarı: observer panic'i → operasyon sürer, core ayakta); `TestTransformPatchSchemaValidatedNoCapabilityFields`; `TestRemoteHookUsesSignedTransport` (T4 imzası + tool-http.v1 envelope reuse assert); `TestUnknownHookPointRejected`; `TestHookCannotGrantCapability` (transform "add push tool" patch'i → şema reject); migration idempotent + down.
- [ ] Fail'i doğrula; minimum beş-nokta dispatch; hook sırası deterministik (kayıt sırası) + dokümante.
- [ ] **Component + fault smoke:** gerçek remote hook worker'ı (local HTTP harness) before_tool'da gerçek deny/allow verir; worker down → policy fail-closed + circuit breaker, control-plane process ayakta ve DİĞER run'lar akar (EXT-005 hook yüzü). Honest ceiling: model-davranış bağı yok (saf infra); canlı model bağı T10 journey'de.

**Verify:** `make verify && make test-component TEST=hooks && make test-fault CASE=extensions`.

### Task 9: Extension SDK (Go/TS + Py minimal) + conformance corpus (TOL-018)

**Files:** Create `packages/extension-sdk/` (Go: define-tool schema emit, `tool-http.v1` signed-invocation verify, normalized result builder, tool_call_id idempotency store helper — T4 imza şemasının ikinci implementasyonu), `sdks/typescript/` içine `@palai/extension-sdk` paketi (aynı dört yüzey; mevcut pnpm workspace), `tests/conformance/tool-sdk/` (paylaşılan JSON fixture corpus'u: şema emit vektörleri, imza doğrulama vektörleri — bilinen-anahtar HMAC, normalized-result eşdeğerlik; `tests/conformance/tools` kalıbı), minimal Python modülü `engines/reference` bağımsız tek dosya (aynı corpus'u geçen verify+schema — TOL-018'in Py ayağı MINIMAL; **KARAR, pin 2026-07-22, §8.6:** tam Py SDK E16).

Semantik (§28.23): SDK tool'a trust ATAYAMAZ; helper'lar yalnız contract'ı doğru üretmeyi kolaylaştırır; corpus üç dilde AYNI fixture dosyalarından koşar — drift = fail (polyglot drift riski, master plan §11 API-012/TOL-018 satırı). Conformance server + packaging/signing DEFER (§2).

- [ ] Failing testler: Go+TS+Py conformance koşucuları aynı corpus'a karşı: `schema-emit` vektörleri byte-eşdeğer JSON Schema üretir; `signature-verify` vektörleri (geçerli/bozuk/expired timestamp) üç dilde aynı kabul/red kümesi; `result-normalize` eşdeğer; TS runner mevcut `pnpm` test zincirinde, Py runner `pyproject` zincirinde.
- [ ] Fail'i doğrula; minimum helper'lar; T4 harness'i kendi el-yazımı imza kodunu SDK Go helper'ıyla DEĞİŞTİRİR (ilk gerçek tüketici — dogfood).
- [ ] **Component smoke:** T4'ün remote-tool harness'i TS SDK ile yeniden yazılmış ikinci bir server varyantıyla da PASS (aynı signed round-trip iki SDK implementasyonuyla). Honest ceiling: Py ayağı minimal verify+schema modülüdür, tam Python SDK değil — evidence "TOL-018 (Go/TS full, Py verify-subset)" der; tam parity E16.

**Verify:** `make verify && make test-component TEST=tool-sdk && pnpm -r test`.

### Task 10: Journey + UAT materialization + `extensibility-0.1.0` evidence → E12 kapanışı (EXT-005, hepsi)

**Files:** Create `tests/e2e/extensions/` journey harness'i, `tests/uat/cases/TOL-008..012/` + `TOL-016..018/` (E10'un 016/017 case.yaml'lerine signed-yarı bölümü eklenir — shared-case disiplini) + `EXT-001..006/` `case.yaml`, `tests/security/extensions/` corpus'un UAT bağı, yeni evidence release `extensibility-0.1.0`; Modify `Makefile` (`uat-extensibility` hedefi; önceki `uat-*` hedefleri DOKUNULMAZ), `tests/uat/evidence.go` (extension-alanı kuralları: advertising kanıtı — provider request'inde şema listesi hash'i; callback-teklik; skill-digest kaydı).

- [ ] Failing test (deterministic tier): `TestExtensibilityJourneyDeterministic` — zincir: (1) tool-set + MCP connection + skill + hook'lu AgentRevision publish (T2 genişlemesi); (2) run başlar, model advertised set'i görür (fake provider şema-doğrulamalı); (3) MCP tool çağrısı + remote async tool çağrısı (202→callback) + web-research fetch'i aynı run'da canonical ledger satırlarıyla; (4) skill talimatı uygulanır ama istediği ekstra capability REDDEDİLİR; (5) before_tool policy hook'u bir çağrıyı görünür DENY eder; (6) MCP server öldürülür → breaker + `tool_unavailable`, run görünür şekilde devam/fail eder, control-plane ayakta (EXT-005); (7) run terminal + evidence. Pass: capability hiçbir katmanda GENİŞLEMEDİ, extension crash core'u DÜŞÜRMEDİ, her extension çağrısı tek dispatch yolundan ledger'lı geçti.
- [ ] Canlı journey (`live-provider` + component-real + security; compose stack, gerçek provider, E09-11 hygiene disiplini): aynı zincir gerçek modelle SPONTAN tool seçimleriyle; 63.4'ün "web research citations" adımı İLK KEZ gerçek research tool'uyla (phase-11 devir 4 kapanışı — automation journey'nin capability-ceiling notu güncellenmez, bu ayrı release'tir); evidence manifest: advertised-şema hash'i, MCP namespacing, callback tekliği, skill digest + scan result, hook deny kaydı, credential absence scan (yeni marker'lar dahil).
- [ ] Evidence verifier: `extensibility-0.1.0` `VerifyRelease` PASS; secret finding 0; EXT-005 crash-isolation kanıtı olmadan PASS SAYILMAZ; TOL-016/017 case'leri E10 ledger-yarısı + E12 signed-yarısı İKİSİ birden referanslı.
- [ ] `make uat-extensibility PROVIDER=provider-one` PASS; deterministik tier fake provider'la CI'da; regresyon: önceki beş `uat-*` hedefi hâlâ PASS (advertising değişikliği eski yolları bozmadı — kritik regresyon yüzeyi).

**Verify:** `make verify && make test-e2e && make test-security && make uat-extensibility PROVIDER=provider-one && make evidence-verify RELEASE=extensibility-0.1.0`.

## 6. Tool-advertising ceiling kararı: E12'DE ÇÖZÜLÜR (T1, açılış dalgası)

**Karar: resolve-in-E12, ilk task.** Gerekçe:
1. **Tel zaten iki uçta bağlı:** `Request.Tools` kontratta (`packages/model-broker/types.go:66`), provider adapter serialize+parse ediyor (`adapters/models/provider_one/adapter.go:249,:139-163`), engine multi-step tool turn'ünü destekliyor (`engines/reference/src/palai_engine/loop.py` `AWAITING_TOOLS`). Eksik yalnız `model_dispatch.go:115-127`'nin `Tools` alanını doldurmaması — bu bir "E16 SDK işi" değil, orchestrator wiring'idir ve E12'nin ana dosyasındadır.
2. **E12'nin bütün değeri buna bağlı:** MCP/remote/skill tool'ları model tarafından GÖRÜLMEZSE, epic'in her canlı kanıtı forced-seam'de kalır — "MCP destekli" iddiası dürüst olamaz. E16'ya ertelemek dört epic'lik LIVE-kanıt borcunu (E08'den beri) iki epic daha taşımak olur.
3. **Borç envanteri kapanır:** E10'un dört gate'li live case'i (`container_kill_test.go:50`, `host_kill_test.go:42`, `restore_test.go:82`, `detached_test.go:33`) + E09 revision-ceiling'in live yarısı (`tools/live/agent_revision_live_test.go:10`) + E11'in single-step sınırı gerçek kanıta döner; `PALAI_LIVE_TOOL_ADVERTISING` repo'dan silinir (yorum referansları dahil — T1 envanteri). Dürüst not: `host-kill-restore` için advertising teli TEK eksik değildir — container-engine live harness'inin runner-yolu pause-boundary wiring'i de gerekir (`host_kill_test.go:50` `t.Fatal`); ikisi birlikte T1'dedir.
4. **Riskler yönetilir:** (a) prompt-injection yüzeyi büyür → description'lar T1'de platform-metni, tenant metni T2 approval'lı; (b) token maliyeti → smoke'lar küçük setle; (c) fake-parity → deterministic testler bit-değişmez regresyon gate'i; (d) eski uat-* regresyonu → T10 zorunlu koşusu.

E16'ya kalan: ikinci provider adapter'ında advertising parity (MOD ailesi) + SDK ergonomileri. Bunlar ceiling değil, genişletmedir.

## 7. Final release check (E12 exit gate)

- [ ] `make verify`, `make test-component`, `make test-e2e`, `make test-fault`, `make test-security` PASS; `make generate` sonrası drift 0.
- [ ] `make uat-extensibility PROVIDER=provider-one` bütün bu-dilim case'leri PASS; hiçbir MCP case'i mock JSON-RPC'yle, hiçbir signed-transport case'i sahte imzayla, hiçbir live case forced-tool'la (T1 sonrası) geçmedi.
- [ ] `make evidence-verify RELEASE=extensibility-0.1.0` PASS; secret + yeni extension-credential finding 0.
- [ ] **Exit gate cümlesi kanıtlı (master plan §8/E12):** malicious skill/MCP metadata capability GENİŞLETEMEDİ (security corpus + EXT-006 + TOL-009/011); extension crash core process'i DÜŞÜRMEDİ (EXT-005 fault-live).
- [ ] `git grep PALAI_LIVE_TOOL_ADVERTISING` = 0 (docs/ dışı). E10 recovery live case'leri gate'siz koşulur: `detached-child-conversation` gerçek PASS; `checkpoint-restore` + `container-kill-recovery` T1b engine-wire tool_call-id follow-up'ına, `host-kill-restore` container-engine harness follow-up'ına SKIP (advertising değil — 2026-07-22 disposition notu §Task 1). `make uat-recovery` yeniden koşuldu (skip'ler temiz).
- [ ] Regresyon: `make uat-local-live`, `make uat-interactive`, `make uat-coding`, `make uat-recovery`, `make uat-automation` (PROVIDER=provider-one) hâlâ PASS.
- [ ] `git status --short` temiz. **M7 kapanır** — "automation beta" iddiası artık verilebilir (E11+E12 tam; phase-11 §6 son maddenin karşı-yarısı).

## 8. Pin adjudication kararları (2026-07-22) — AMEND-LATER'lar KAPANDI

1. **E11 merge tip + evidence — KAPANDI:** main = `61e7c48` (E11 T1-T7 COMPLETE); `make uat-automation` + `automation-0.1.0` ağaçta doğrulandı; migration zinciri ucu `000023_inbound_trigger_auth` (E11 T6/T7 migration indirmedi) → ilk E12 migration 000024. §1 gate satırları pinli.
2. **Migration merge sırası — DİSİPLİN PİNLENDİ:** atamalar §1'de (T2=24, T4=25, T5=26, T7=27, T8=28); ikinci dalga paralel worktree'lerde koştuğu için sapma MERGE ANINDA henüz-merge-edilmemiş migration'ların yeniden numaralandırılmasıyla emilir (E11 §1 kalıbı); zincirde boşluk yasak.
3. **Web-research search-provider — KARAR (epic-level, dürüst minimal):** E12 çekirdeği fetch+citations'tır, HİÇBİR search-provider bağımlılığı almaz; gerçek arama backend'i (Brave/SerpAPI vb. Connection + secret + egress policy) **E17 (KNO)** işidir. T3 bloklanmaz; iddia "web research (fetch+cite)", "web search" değil (§2 Dahil-değil satırı güncellendi).
4. **MCP stdio container — KARAR (reuse-with-named-gap):** `adapters/sandboxes/oci` driver'ı MCP'ye özel label sınıfıyla DOĞRUDAN yeniden kullanılır; RunnerGateway'e sokulmaz (E09 T1 invariantı korunur); `host_quarantine` (000017) MCP'ye genişletilmez — failure yüzeyi connection circuit breaker. Adlandırılmış boşluk = label-scoped orphan-MCP-container sweep, T5'te kapatılır (test listesinde). Fallback (host child process + rlimit) yalnız driver-attach engeli çıkarsa açık kararla. Tam metin T5 semantiğinde.
5. **Üçüncü-parti gerçek MCP server smoke'u — AÇIK KALAN TEK KULLANICI OPSİYONU (pin'i bloklamaz):** default plan test-server'lardır (supply-chain); kullanıcı isterse T10'da digest-pinli bir public server (ör. referans filesystem server) tek smoke olarak eklenir.
6. **TOL-018 Python ayağı — KARAR (master planla tutarlı):** minimal Py verify+schema modülü (paylaşılan corpus'u geçer) + FULL Go/TS. Master plan §8/E12'nin "TypeScript/Python/Go schema/signature parity" satırı paylaşılan-corpus eşdeğerliğiyle sağlanır; TAM Py SDK ergonomisi E16'nındır ("SDK parity ve provider completeness" epic'i; M10 "3 SDK"). Evidence "TOL-018 (Go/TS full, Py verify-subset)" der.
7. **M7/M8 + master-plan pointer — KAPANDI (bu doc) / TEK SATIR DIŞARIDA:** master plan §7 doğrulandı — M7 = automation & extensions (E11+E12, satır 230), M8 = governance (E13, satır 231); bu plandaki bütün milestone iddiaları M7 (başlık paragrafı + §7 "M7 kapanır"). Master plan §8/E12 child-plan pointer'ı (satır 456) hâlâ `phase-12-extensions.md` yazar — gerçek dosya `phase-12-extensibility.md`; düzeltme tek satırlık master-plan dokunuşudur, bu adjudication'ın edit kapsamı dışındadır (yalnız bu doc) ve sonraki master-plan touch'ında yapılır (phase-11 devir 9 kalıbı). Pointer uyuşmazlığı pin'i BLOKLAMAZ.
8. **AgentRevision genişlemesinin E11 testiyle bilinçli çakışması — KAYITLI:** `TestUnknownRevisionFieldsRejected` E12 alanlarını kabul edecek şekilde T2'de bilinçli güncellenir (phase-11 §7 devir 3'ün kapanışı); hâlâ-bilinmeyen alan reject kalır — E17 knowledge alanları için aynı kalıp tekrarlanır.
