id: UI-002
name: console-authoritative-approval-detail-operation-branch-request-hash-and-display-never-replaces-it
proof_class: e2e-deterministic
provider: fake
input: "The console's approval UI shows the AUTHORITATIVE, model-independent detail the canonical approval.requested.v1 event ACTUALLY carries (§47.2, UI-002, packages/coordinator/publication.go): operation, branch, and request_hash — each surfaced from the event, not the model's prose. There is NO publications/approvals READ endpoint (apps/control-plane/api/router.go), so the event payload is all a real client receives. The proposal-supplied `display` string is rendered in a SEPARATE, explicitly non-authoritative region and NEVER substitutes for the authoritative detail: a soothing 'everything looks fine, safe to approve' display does not stand in for the operation/branch the operator is authorizing. Approve drives a durable approve command through the /v1 relay and the run proceeds through recovery to completed; deny BLOCKS the side effect (the run terminates canceled, the push tool-completed frame never arrives). PUBLIC-API GAP (the E13-T10 modelRoutes-gap pattern, filed in the console README): richer approval detail — action/args/diff/destination/risk/expiry, or a publications read endpoint — is NOT carried by the event today; the console renders it when the API emits it. This is E18/hardening input, not a console defect. PROOF SEAM: playwright against the built console over a deterministic FAKE /v1 upstream (tests/fake-control-plane.mjs) that scripts approval.requested.v1 with its REAL {publication_id, operation, branch, request_hash, display} shape and a deliberately reassuring display string. §6 operator leg: exercising the exact-approval UI against a real run on a deployed stack is operator work above this automated ceiling."
expect_status: completed
proof:
  - "apps/web-console/tests/journey.spec.ts:UI-002: the approval UI shows the authoritative operation/branch/request_hash from the canonical event — the proposal display string does not replace them"
  - "apps/web-console/tests/journey.spec.ts:approve proceeds through recovery to a completed run with a downloadable artifact"
  - "apps/web-console/tests/journey.spec.ts:deny blocks the operation — the run terminates canceled, the push never completes"
