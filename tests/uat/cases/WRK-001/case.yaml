id: WRK-001
name: worker-outbound-enrolls-with-one-time-token-and-short-lived-identity-no-inbound-port
proof_class: component-real
provider: fake
input: "A capability worker ENROLLS the runner way (§31, §3 seam): it DIALS the control-plane capability gateway OUTBOUND with a ONE-TIME token, the token is spent on first use, and it receives a SHORT-LIVED workload identity — NO inbound port is ever opened to the worker (the gateway is the only listener; it dials nothing back). A worker may only enroll for a capability the control plane types (Catalog); apple-build is ABSENT, so it cannot enroll. LOCAL proof: the component-real store enrolls a swift-toolchain worker (RLS-scoped) and the two-sided-live proof runs a REAL, separately-executed macOS-native worker PROCESS that enrolls outbound over the gateway and receives a workload token. §6 leg 3: a real Xcode+signing worker is operator work — apple-build stays disabled."
expect_status: completed
proof:
  - apps/control-plane/internal/workers/workers_component_test.go:TestWorkerEnrollTypedDispatchAndArtifactRoundTrip
  - apps/control-plane/internal/workers/catalog_test.go:TestAppleBuildCapabilityIsAbsentEverywhere
  - apps/control-plane/internal/workers/live/worker_live_test.go:TestTwoSidedTypedJobRoundTrip
