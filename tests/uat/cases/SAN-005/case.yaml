id: SAN-005
name: snapshot-restore-checksums-match-create-secrets-excluded
proof_class: unit
provider: fake
input: "a workspace snapshot restore reproduces file/index/tree checksums byte-for-byte equal to create; the secret + credential are recorded as exclusions and never enter the archive bytes, while the .git dir is preserved for post-terminal publication"
expect_status: completed
proof:
  - adapters/sandboxes/oci/snapshot/archive_test.go:TestSnapshotRestoreChecksumsMatchCreate
# ceiling: the CREATE side + its checksum are proven by E09 T1; checkpoint/snapshot envelope-encryption-at-rest is E13 (E10 proves integrity + secret-absence, not at-rest encryption)
